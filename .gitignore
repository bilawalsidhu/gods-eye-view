# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Secrets and local config
.env
.env.local

# Tooling state
.playwright-mcp/
.omc/
.remember/
.codegraph/
.DS_Store

# Local data cache: the GeoNames dump and its ETag, and upstream.sqlite3, which is where
# response caches and rate-limit floors survive a restart. Both are settings, so a deployment
# may point them elsewhere; this covers the defaults.
.cache/
