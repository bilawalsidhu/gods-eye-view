# Closeout audit — 2026-09-19 (branch `ondemand-serverless`)

Run of 2026-09-19 01:20–02:20Z in the persistent session workspace. Every timestamp is UTC. **No secret VALUE
appears in this file** — environment-variable NAMES only. Results of tasks A, C, D, E and G are recorded here;
the commit sha, push output and export checksums of tasks F and H cannot precede the commit that contains this
file and are reported in the run's final response (and, for the push, by `git log` / `git ls-remote`).

## A. HEAD, flow-ID variable and the exhaustive env-var table

### A.1 HEAD

`git log --oneline -8` at the start of the run (01:22:31Z):

```
68a51bf docs(handover): deployment outcome 2026-09-18
7ae8a50 fix(vercel): route multi-segment /api/* paths to the catch-all function
a62d4a7 docs: close-out decisions and handover 2026-09-18
6320b58 docs(ondemand): live API re-validation §18, ONDEMAND_SPATIAL_WORKFLOW_ID, registration pack, plugin resolution, .env.example
bcf881f feat(entity-chat): OnDemand chat overlay on aircraft/vessel/satellite selection + x-ondemand-key override
1813f0d feat(voice): OnDemand-only turn-based voice mode + /api/ondemand/workflow/{execute,status,logs,outputs,stream}
ab23c7b feat(tools): OnDemand-callable tool plugins behind /api/tools (satellites, flights, military, vessels, traffic, earthquakes)
4dc98fc docs(movement): MOVEMENT-LAYERS design + live verification, .env.example variables, CHANGELOG, serverless-limitations update
```

HEAD was **`68a51bf2232d4b1608dca53f7fa7097e5ca16041`, not `4dc98fc`**: the branch tip already carried seven later
commits — the tools / voice / entity-chat features (`ab23c7b` `1813f0d` `bcf881f`), the live API re-validation
(`6320b58`), the close-out docs (`a62d4a7`), the real-Vercel routing fix (`7ae8a50` — Vercel matches
`api/[...route].js` for ONE path segment only; a `vercel.json` rewrite + adapter query-strip made the
multi-segment MOVEMENT routes reachable) and its handover (`68a51bf`). `origin/ondemand-serverless` = `68a51bf`
(pushed). Working tree clean. The checkout was edited in place, as instructed for the "otherwise report actual and
continue" case; `4dc98fc` is an ancestor of HEAD.

### A.2 Flow-ID variable — verbatim

- Environment-variable NAME for the OnDemand workflow/flow id: **`ONDEMAND_SPATIAL_WORKFLOW_ID`** (canonical,
  checked first), accepted alias **`ONDEMAND_SPATIAL_FLOW_ID`** — `server/ondemand/config.js` `WORKFLOW_ID_ENV`
  (`api/ondemand/_config.js` is the re-export shim every function imports; it holds no literal of its own beyond
  the documentation table).
- Code default, quoted character for character from `server/ondemand/config.js` line 423:
  `spatialFlowId: '6aace534859f7b0abb53d99a',` — **24 hexadecimal characters**. The 26-character spelling
  `6aace534859f9f7b0abb53d99a` given in the brief does **not** occur anywhere in the code (`git grep` at HEAD: 0
  hits outside prose that warns against it); the code comment records that it answers 404 upstream. Do not
  provision it.
- Resolution order (`reconcile`): `ONDEMAND_SPATIAL_WORKFLOW_ID` → `ONDEMAND_SPATIAL_FLOW_ID` → the default above.
  Neither name is on the Vercel project, so every deployment resolves `config.spatialFlowId.source = "default"`.
- The flow VERSION row is the one alias-first row: `GODS_EYE_FLOW_VERSION` → `ONDEMAND_SPATIAL_FLOW_VERSION` →
  `'1'`. Both are on the project, so the deployment reports `source = GODS_EYE_FLOW_VERSION`, `resolvedVia = alias`
  until the alias is deleted.

### A.3 Env-var table (every name the deployment reads)

Method: `grep` of `process.env.NAME` / `import.meta.env.NAME` across `server/`, `api/`, `src/` (83 direct names)
plus the helper-read names (`env[name]` / `envNumber(name)` / `reconcile(name)` in `server/ondemand/config.js`,
`server/providers/aircraft/opensky.js`, `server/providers/vessels/{ais-serverless,kv-store}.js`,
`server/providers/overpass/constants.js`, `server/providers/cctv/catalog.js`, `server/sources/nasa-firms.js`),
reconciled against `.env.example`. Vercel column = the step-1 baseline list of the 20 variables present on
`ondemand-eand-spatial` (ids are Vercel metadata; values never read).

| Canonical name | Accepted aliases | Required / optional | Code default (non-secret) | Layer / function / adapter | Vercel project |
|---|---|---|---|---|---|
| `ONDEMAND_API_KEY` | — | required for a configured proxy (unkeyed = "not configured" health) | — (secret; no default) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_BASE_URL` | `ONDEMAND_API_BASE` | optional | `https://api.on-demand.io` | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_API_BASE` | alias of `ONDEMAND_BASE_URL` | optional | (alias) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_REASONING_ENDPOINT_ID` | `ONDEMAND_ENDPOINT_ID` | optional | `low` (a reasoningMode tier, not an endpoint) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_FULFILLMENT_ENDPOINT_ID` | `ONDEMAND_ENDPOINT_ID` | optional | `predefined-gpt-5.6-luna` (ASK tier) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_ENDPOINT_ID` | alias of both endpoint ids | optional | (alias) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_REASONING_MODE` | — | optional (validated: low / medium / high) | unset → field omitted upstream | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_SPATIAL_WORKFLOW_ID` | `ONDEMAND_SPATIAL_FLOW_ID` (canonical checked first) | optional | `6aace534859f7b0abb53d99a` (FLOW_DEFAULTS.spatialFlowId) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow); `api/ondemand/workflow.js` execute / selftest step 8 | MISSING |
| `ONDEMAND_SPATIAL_FLOW_ID` | alias of `ONDEMAND_SPATIAL_WORKFLOW_ID` | optional | (alias) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | MISSING |
| `ONDEMAND_SPATIAL_FLOW_VERSION` | `GODS_EYE_FLOW_VERSION` (alias checked FIRST) | optional | `1` (FLOW_DEFAULTS.flowVersion) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `GODS_EYE_FLOW_VERSION` | alias of `ONDEMAND_SPATIAL_FLOW_VERSION` (wins while set) | optional | (alias) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | **PRESENT** |
| `ONDEMAND_SPATIAL_AGENT_ID` | — (the retired plugin-ids name is DENIED) | optional | `[]` (no default pluginIds) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | MISSING |
| `ONDEMAND_REQUEST_TIMEOUT_MS` | — | optional | proxy default (server/ondemand/config.js) | `api/ondemand/*.js` via server/ondemand/config.js (chat, health, media, selftest, sessions, stt, tts, workflow) | MISSING |
| `ONDEMAND_SELFTEST_TOKEN` | — | required only to expose `GET /api/ondemand/selftest` | — (secret; unset = selftest 404/disabled) | `api/ondemand/selftest.js` | **PRESENT** |
| `VITE_SERVERLESS_MODE` | `SERVERLESS_MODE` (server twin), `VERCEL` (auto) | required for the serverless UI build | unset (false) | Vite build (browser bundle, `import.meta.env`) + server/serverless/app.js | **PRESENT** |
| `SERVERLESS_MODE` | see `VITE_SERVERLESS_MODE` | optional | unset | server/serverless/app.js (all functions) | MISSING |
| `VERCEL` | platform-set | n/a | set by Vercel | server/serverless/app.js (serverless-mode detection) | MISSING |
| `OPENSKY_CLIENT_ID` | — | optional (enables OAuth2 client-credentials) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/aircraft/opensky.js (`/api/opensky`) | MISSING |
| `OPENSKY_CLIENT_SECRET` | — | optional (with `OPENSKY_CLIENT_ID`) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_AUTH_MODE` | — | optional | auto | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_USERNAME` | — | optional (legacy basic auth) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_PASSWORD` | — | optional (legacy basic auth) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_TIMEOUT_MS` | — | optional | `6000` (1000–30000) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_RETRIES` | — | optional | `0` (0–2) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_BREAKER_MS` | — | optional | `600000` (10 min) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_BBOX_DEGREES` | — | optional | `1.5` | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `OPENSKY_FALLBACK_RADIUS_NM` | — | optional | `250` | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js | MISSING |
| `AIRPLANES_LIVE_ENABLED` | — | optional (opt-in third fallback) | unset (off) | `api/[...route].js` (catch-all → server/serverless/app.js) → opensky.js, aircraft/adsb-lol.js (`/api/adsblol/mil`) | MISSING |
| `AISSTREAM_API_KEY` | — | optional (live AIS; without it: AISHub or demo replay) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/vessels/ais-live.js / ais-serverless.js (`/api/ais-live`) | MISSING |
| `AISSTREAM_URL` | — | optional | `wss://stream.aisstream.io/v0/stream` | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-live.js | MISSING |
| `AISSTREAM_BOUNDING_BOXES` | — | optional | scene bbox | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-live.js | MISSING |
| `AISSTREAM_MESSAGE_TYPES` | — | optional | position reports | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-live.js | MISSING |
| `AISSTREAM_SILENCE_TIMEOUT_MS` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-live.js | MISSING |
| `AISSTREAM_COLLECT_MS` | — | optional | `8000` | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-serverless.js (bounded collector) | MISSING |
| `AISSTREAM_COLLECT_QUIET_MS` | — | optional | `2500` | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-serverless.js | MISSING |
| `AISSTREAM_SNAPSHOT_TTL_MS` | — | optional | `25000` | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-serverless.js | MISSING |
| `AISHUB_USERNAME` | — | optional (keyless delayed AIS fallback) | — (account name) | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/ais-serverless.js | MISSING |
| `KV_REST_API_URL` | `UPSTASH_REDIS_REST_URL` | optional (shared AIS snapshot cache) | unset (per-instance memory) | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/kv-store.js | MISSING |
| `KV_REST_API_TOKEN` | `UPSTASH_REDIS_REST_TOKEN` | optional | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/kv-store.js | MISSING |
| `UPSTASH_REDIS_REST_URL` | alias of `KV_REST_API_URL` | optional | (alias) | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/kv-store.js | MISSING |
| `UPSTASH_REDIS_REST_TOKEN` | alias of `KV_REST_API_TOKEN` | optional | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → vessels/kv-store.js | MISSING |
| `TOMTOM_API_KEY` | — | optional (live traffic; keyless = DEGRADED simulation) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/traffic.js (`/api/tomtom/*`) | MISSING |
| `TOMTOM_DAILY_TILE_BUDGET` | — | optional | `40000` | `api/[...route].js` (catch-all → server/serverless/app.js) → traffic.js | MISSING |
| `TOMTOM_DAILY_REQUEST_BUDGET` | — | optional | `2000` | `api/[...route].js` (catch-all → server/serverless/app.js) → traffic.js | MISSING |
| `OVERPASS_ENDPOINTS` | `OVERPASS_UPSTREAMS` (2026-09-18 name) | optional | 5 public mirrors (kumi.systems, private.coffee, overpass-api.de, lz4., z.) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/overpass/constants.js + transport.js (`/api/overpass`, Street Traffic roads) — **new 2026-09-19** | MISSING |
| `OVERPASS_UPSTREAMS` | alias of `OVERPASS_ENDPOINTS` | optional | (alias) | `api/[...route].js` (catch-all → server/serverless/app.js) → overpass/constants.js; `road_network_status` tool | MISSING |
| `OVERPASS_MIRROR_TIMEOUT_MS` | — | optional | `12000` (1000–60000) — **new 2026-09-19** | `api/[...route].js` (catch-all → server/serverless/app.js) → overpass/constants.js | MISSING |
| `OVERPASS_TOTAL_TIMEOUT_MS` | — | optional | `40000` (2000–120000) — **new 2026-09-19** | `api/[...route].js` (catch-all → server/serverless/app.js) → overpass/constants.js | MISSING |
| `ROAD_NETWORK_SOURCE` | — | optional | `overpass` (or `off`) | `api/[...route].js` (catch-all → server/serverless/app.js) → overpass/constants.js; server/tools/traffic.js | MISSING |
| `NASA_FIRMS_MAP_KEY` | — | optional (Fires row, `/api/sources/fires`) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/sources/nasa-firms.js | MISSING |
| `FIRMS_MAP_KEY` | — | optional (legacy `/api/firms` provider) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/firms.js | MISSING |
| `LL2_API_TOKEN` | — | optional | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/space/launch-library.js | MISSING |
| `TFL_APP_KEY` | — | optional | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/cctv/sources.js | MISSING |
| `OPENAI_API_KEY` | — | optional (voice/HUD summary; OnDemand-only voice does not need it) | — (secret) | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/openai/{hud-summary,realtime}.js | MISSING |
| `OPENAI_HUD_SUMMARY_MODEL` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/hud-summary.js | MISSING |
| `OPENAI_REALTIME_MODEL` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `OPENAI_REALTIME_MODEL_MINI` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `OPENAI_REALTIME_VOICE` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `OPENAI_REALTIME_REASONING_EFFORT` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `OPENAI_REALTIME_CONTEXT_TOKENS` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `OPENAI_REALTIME_CONTEXT_RETENTION` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → openai/realtime.js | MISSING |
| `GEV_RATELIMIT_GOOGLE_PER_MIN` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/places/google.js | MISSING |
| `GEV_RATELIMIT_OPENAI_PER_MIN` | — | optional | provider default | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/openai/rate-limit.js | MISSING |
| `GOOGLE_MAPS_API_KEY` | — | optional (browser Places/Maps) | — (secret) | local dev only (server/standalone/vite.config.js / src/main.js) — not read by the deployment; `src/main.js` reads it at BUILD time via `import.meta.env` | MISSING |
| `GOOGLE_MAPS_SERVER_API_KEY` | — | optional | — (secret) | src/keySetupCore.mjs (key-setup UI only) | MISSING |
| `CESIUM_ION_TOKEN` | — | optional (Cesium ion assets) | — (secret) | local dev only (server/standalone/vite.config.js / src/main.js) — not read by the deployment; `src/main.js` at BUILD time | MISSING |
| `CCTV_SOURCES_FILE / CCTV_SOURCES_JSON / CCTV_AUSTIN_ROWS_URL / CCTV_CALGARY_ROWS_URL / CCTV_*_MAX_SOURCES / CCTV_*_ENABLED / CCTV_*_DISTRICTS / CCTV_*_SOURCES_FILE / CCTV_FORCE_AUSTIN / CCTV_PREFER_AUSTIN / CCTV_MAX_SOURCES / CCTV_GROUND_HEIGHTS_FILE` | — | optional (CCTV source-pack tuning; 30 names) | `*_ENABLED` default `1`; others provider defaults | `api/[...route].js` (catch-all → server/serverless/app.js) → server/providers/cctv/{catalog,sources,groundHeights}.js | MISSING (none of these) |
| `VITE_AIS_LIVE_API_URL / VITE_AIS_LIVE_MAX_ROWS / VITE_AIS_LIVE_LABEL_MAX_ROWS` | — | optional (build-time) | same-origin `/api/ais-live`; row caps | Vite build (browser bundle, `import.meta.env`) (src/data/aisLiveVessels.js, src/standalone/*) | MISSING (none of these) |
| `GEV_ALLOC_* (CHUNK, CHUNKS, ENTRIES, PROFILE, SOLVE_MS, STABILIZATION_CHUNKS, WARMUP)` | — | optional (allocation micro-benchmark) | test defaults | src/overlays/worldOverlayAllocation.worker.mjs (test/benchmark only) | MISSING (none of these) |
| `GEV_KEY_SETUP_EXTERNAL_KEYS / GEV_LAUNCHER / GEV_HARDEN_FAILED / GEV_STORE_UNREADABLE / PINOKIO_SHARE_VAR` | — | optional | — | local dev only (server/standalone/vite.config.js / src/main.js) — not read by the deployment (Pinokio key-setup flow) | MISSING (none of these) |
| `HOST / PORT` | — | optional | `0.0.0.0` / `3000` (emulator) | server/serverless/dev-server.mjs, server/standalone/vite.config.js (local only) | MISSING (none of these) |
| `BASE_URL / DEV` | Vite built-ins | n/a | Vite | Vite build (browser bundle, `import.meta.env`) | MISSING (none of these) |

Present on the Vercel project but **read by nothing in this code base** (left over from the `ondemand-eand-spatial`
exec-brief app or denied): `ASK_ENABLED`, `CI`, `ONDEMAND_KNOWLEDGE_PLUGIN_IDS`, `ASK_THE_DEAL_ALLOWED_ORIGIN`, `DOC_BUSINESS_PLAN_URL`, `DOC_BUSINESS_PLAN_PDF_URL`, `DOC_MOU_URL`, `DOC_GANTT_URL`, `ELEVENLABS_API_KEY` — `ONDEMAND_KNOWLEDGE_PLUGIN_IDS` and
`ELEVENLABS_API_KEY` are on the proxy's deny-list and are never read; `ASK_ENABLED`, `ASK_THE_DEAL_ALLOWED_ORIGIN`,
`DOC_*` and `CI` belong to the previous application on that project.

Missing on the project and needed for LIVE movement rows: `OPENSKY_CLIENT_ID`, `OPENSKY_CLIENT_SECRET`,
`AISSTREAM_API_KEY`, `TOMTOM_API_KEY`; optional: `ONDEMAND_SPATIAL_WORKFLOW_ID` (or the code default),
`OVERPASS_ENDPOINTS` (a private/self-hosted mirror first), KV credentials, `AISHUB_USERNAME`.

## B. Defaulted rename decisions (recorded 2026-09-19)

Both decisions are recorded here and in `docs/audit/rebrand-grep-report-2026-09-18.md` ("Decisions (2026-09-19)").
No code changed for this task.

| # | Group | Decision | Rationale |
|---|---|---|---|
| (i) | 1,250 persisted-state / registered-client identifiers (storage keys, client ids, `x-…` extension keys, externalUserId prefixes already migrated where user-visible) | **RETAIN UNCHANGED — no storage-key migration** | Invisible to users; renaming persisted keys would wipe every existing user's saved state (layer prefs, first-run state, caches) without a migration path. Revisit only if a storage-key migration is scheduled. |
| (ii) | 64 frozen v1 workflow-prompt strings ("God's Eye pipeline" inside the nine node prompts of workflow `6aace534859f7b0abb53d99a` v1 and its export `docs/ondemand-workflows/ondemand-spatial-advanced-v1.json`) | **LEAVE IN v1 — reword only in workflow v2** | Published workflow versions are immutable on the platform; the display name was already changed (2026-09-18T10:41:47Z, id unchanged). The wording moves with the next functional change (v2). |

## C. Street Traffic road network — configurable Overpass mirrors

Scope: the Overpass/OSM road-network adapter only. CelesTrak, OpenSky, adsb.lol/adsb.fi, AISStream, TomTom, the
OnDemand proxy, the deny-list and the function count are untouched (the two-line `scripts/package-boundaries.json`
change only declares the transport's new dependency on the shared helper for the two packages that bundle it).

Files touched: `server/providers/overpass/constants.js`, `server/providers/overpass/transport.js`,
`server/providers/overpass.js`, `server/tools/traffic.js` (description + override detection),
`src/layers/traffic/{ingestion,controls,state,lifecycle,viewport}.js`, `src/tooling/overpassMirrors.test.mjs`
(new), `src/overpassProxy.test.mjs` + `src/tooling/toolsTraffic.test.mjs` (two pinned literals: default mirror
count 4→5, `fromEnv` gains `OVERPASS_ENDPOINTS`), `scripts/package-boundaries.json`, `.env.example`,
`docs/SERVERLESS_LIMITATIONS.md` (§10 count, new §11).

Reachability probe (`[out:json][timeout:25];node(1);out;`, POST, shared UA, `Accept: application/json`, 12 s):

| Mirror | agent sandbox 01:28:41Z | Vercel Sandbox (sb-2wb9cvfm6b9q) 01:48:38Z | encoded position |
|---|---|---|---|
| `https://overpass.kumi.systems/api/interpreter` | 200 · 1,356 ms · `osm_base 2026-06-01` | 200 · 2,341 ms · `osm_base 2026-07-28` | 1 |
| `https://overpass.private.coffee/api/interpreter` | timeout 12.5 s with UA+Accept; 200 in 1.7 s / 4.7 s on two other probes (round-robin members lag) | timeout 12.0 s | 2 |
| `https://overpass-api.de/api/interpreter` | 406 · 575 ms (HTML) | 406 · 331 ms | 3 (kept, last) |
| `https://lz4.overpass-api.de/api/interpreter` | 406 · 825 ms | 406 · 328 ms | 4 (kept, last) |
| `https://z.overpass-api.de/api/interpreter` | 406 · 673 ms | 406 · 439 ms | 5 (kept, last) |

Default order encoded in `OVERPASS_DEFAULT_UPSTREAMS`: kumi.systems → private.coffee → overpass-api.de →
lz4.overpass-api.de → z.overpass-api.de (reachable first; refusing mirrors kept last rather than dropped).

Behaviour:

- `OVERPASS_ENDPOINTS` (comma-separated, trimmed, empty entries ignored, duplicates collapse) →
  `OVERPASS_UPSTREAMS` (alias) → default. `resolveOverpassEndpoints(env)`; `roadNetworkConfig()` reports
  `endpointsSource` and `fromEnv.OVERPASS_ENDPOINTS` / `.OVERPASS_UPSTREAMS`.
- Every request: `POST`, body `data=<query>`, `Content-Type: application/x-www-form-urlencoded`,
  `User-Agent: ondemand-spatial/0.1 (+https://github.com/bilawalsidhu/gods-eye-view)` (the ONE shared
  `PROVIDER_USER_AGENT`), `Accept: application/json`.
- Rotation through the shared helper `fetchUpstream()` (one call per mirror, `retries: 0`, per-mirror budget
  `OVERPASS_MIRROR_TIMEOUT_MS` = 12 s, whole-rotation budget `OVERPASS_TOTAL_TIMEOUT_MS` = 40 s): HTTP 406 / 429 /
  any 5xx (+ 403 / 408), rate-limit or runtime-error bodies, oversized bodies, network errors and timeouts move on;
  a 400-class query refusal also rotates but, if every mirror agrees, is passed through as upstream's verdict.
- All mirrors failed → transport payload `provider = { status: 'degraded', source: 'Overpass', error: <reason> }`
  with reason `all 5 mirrors failed · last: kumi.systems HTTP 406` (or `3 of 5 mirrors failed, 2 skipped (time
  budget) · last: private.coffee timed out after 12 s`); proxy answers **HTTP 503** + `X-Provider-Status: degraded`,
  `X-Provider-Source: Overpass`, `X-Provider-Error`, JSON `{ error, provider, failures[] }` (never a bare 502,
  never a mirror's 406 page); last-good roads from memory/disk still win (`stale`). Successful answers now carry
  `X-Provider-Status: live` + `X-Provider-Source: Overpass (<mirror>)`.
- Client: `describeRoadError()` uses the proxy's reason verbatim; `getStats()` presents `status`/`providerStatus`
  `degraded`, `providerError` = reason, source `Overpass`, no `error` → the row reads exactly
  `DEGRADED · Overpass · <reason>` (` · retrying` appended only while the bounded retry is in flight).

Tests added (`src/tooling/overpassMirrors.test.mjs`, 6 tests, all passing):

1. `OVERPASS_ENDPOINTS unset: the default mirror list, in the verified priority order`
2. `OVERPASS_ENDPOINTS parsing: comma-separated, trimmed, empty entries ignored, order kept` (+ alias precedence, garbage → default, `overpassMirrorLabel`)
3. `rotation: 406 → 429 → 503 → timeout → the fifth mirror answers and its data is returned`
4. `all mirrors fail → status degraded with a reason that names the count and the last failure` (+ reason grammar incl. the time-budget cut, all-network throw carries the status, 400-everywhere is passed through)
5. `proxy: every mirror failing answers HTTP 503 with X-Provider-Status degraded and the reason (never a 502 or a 406 page)` (+ the client's verbatim reason)
6. `every mirror request carries the shared User-Agent, Accept: application/json and a form-encoded POST body`

## D. Registration pack — brand grep

`grep -rniE "GodsEye|Gods Eye|God's Eye|gods-eye|gods_eye"` over `docs/registration/`, `docs/ondemand-skills/`,
`docs/ondemand-tools/`, `docs/ondemand-workflows/` and `docs/audit/dashboard-registration-pack.md`
(01:40:17Z): **`docs/registration/ONDEMAND_REGISTRATION_PACK.md` — 0 hits.** 66 hits elsewhere:
`dashboard-registration-pack.md` 23 and `ondemand-workflows/README.md` 10 + `ondemand-skills/README.md` 5 (all
old→new rename tables and "created as GodsEye Advanced Spatial Workflow" history rows),
`ondemand-spatial-advanced-v1.json` 22 (the frozen v1 node prompts — decision B(ii) — plus the `_export.rename`
record), `verification-2026-09-18.json` 2 / `contract-baseline*.json` 3 (immutable run records naming the
workflow as it was called at the time), `ondemand-spatial-structured-response-writer.md` 1 (quotes the v1
`runMeta`). None is user-facing wording in the pack itself → **no change, hits retained** as identifiers / history
under decision B.

## E. Gate set (closeout tree, 01:40:43–01:43:11Z)

| Gate | Command | Result |
|---|---|---|
| format | `node scripts/format.mjs --check` | ✓ 964 source files |
| boundaries | `npm run check:boundaries` | ✓ (import directions 760 modules; package ownership — `overpass-provider` and `military-installations-provider` now own `server/providers/common/upstream.js`) |
| unit | `npm test` | **4,352 tests · 4,351 pass · 0 fail · 1 skipped** (the pre-existing Node-24-calibrated allocation gate; +6 new tests over the tip's 4,346) |
| ondemand | `npm run test:ondemand` | 219 pass / 0 fail (the tip's count; the 192 baseline predates `6320b58`) |
| serverless | `npm run test:serverless` | 57 pass / 0 fail (the tip's count; the 47 baseline predates the adapter tests of `7ae8a50`) |
| contract | `npm run ondemand:contract` | not run — requires `ONDEMAND_API_KEY` in the environment (not present in this runtime) |
| build | `npm run build` | ✓ exit 0 in 6 s (`✓ built in 4.83s`, `dist/` 31 MB) — after `npm ci` (3 s) restored the `node_modules/.bin` symlinks the workspace restore had dropped |

Serverless functions: **exactly 9** — `api/[...route].js`, `api/ondemand/chat.js`, `api/ondemand/health.js`,
`api/ondemand/media.js`, `api/ondemand/selftest.js`, `api/ondemand/sessions.js`, `api/ondemand/stt.js`,
`api/ondemand/tts.js`, `api/ondemand/workflow.js` (`api/ondemand/_config.js` is an underscore-prefixed private module).

## G. Sandbox preview verification

Sandbox `sbx_zf8FIdgiADdO5PKDGDoFHtp74d03`, Node 24.14.1, port 3000 → `https://sb-2wb9cvfm6b9q.vercel.run`
(created 01:45:59Z; `npm ci` 01:46:49Z, `npm run build` 01:46:55–01:47:10Z, `node server/serverless/dev-server.mjs`
listening 01:47:22Z). Runtime credentials were injected **server-side only** as process environment of the dev
server (`ONDEMAND_API_KEY`, `ONDEMAND_BASE_URL` from this runtime's secrets, a run-generated
`ONDEMAND_SELFTEST_TOKEN`, `ONDEMAND_SPATIAL_FLOW_VERSION=1`); none is in `.env`, the bundle or any log.

### G.1 `GET /api/ondemand/health?envNames=1` — 01:47:39Z, HTTP 200 (2.1 s)

`ondemand: healthy`, `chat: healthy`, `speech: healthy`, `media: healthy`,
`workflow: healthy`, `configured: true`, `message: "OnDemand API reachable; chat probe succeeded."`;
`config.flowVersion` = `{"configured": true, "source": "ONDEMAND_SPATIAL_FLOW_VERSION", "resolvedVia": "canonical", "canonical": "ONDEMAND_SPATIAL_FLOW_VERSION", "alias": "GODS_EYE_FLOW_VERSION"}`;
`config.spatialFlowId` = `{"configured": true, "source": "default", "resolvedVia": "default", "canonical": "ONDEMAND_SPATIAL_WORKFLOW_ID", "alias": "ONDEMAND_SPATIAL_FLOW_ID"}` (→ the code default
`6aace534859f7b0abb53d99a`); `config.baseUrl.source = ONDEMAND_BASE_URL`,
`config.apiKey.configured = true`; `env.names` = `ONDEMAND_API_KEY ONDEMAND_BASE_URL ONDEMAND_SELFTEST_TOKEN ONDEMAND_SPATIAL_FLOW_VERSION SERVERLESS_MODE VITE_SERVERLESS_MODE`; `env.sources` =
`{"apiKey": "ONDEMAND_API_KEY", "baseUrl": "ONDEMAND_BASE_URL", "reasoningEndpointId": "default", "fulfillmentEndpointId": "default", "defaultPluginIds": "unset", "spatialFlowId": "default", "reasoningMode": "unset", "flowVersion": "ONDEMAND_SPATIAL_FLOW_VERSION", "requestTimeoutMs": "default"}`. (On the real project the flow-version row reads
`GODS_EYE_FLOW_VERSION` / `alias` because both names are set there.)

### G.2 `GET /api/ondemand/selftest` (with `x-selftest-token`) — 01:47:41Z, HTTP 200 (39.2 s)

**9 passed / 0 failed / 1 skipped** (totalMs 38972).

| Step | Name | Status | ms |
|---|---|---|---|
| 1 | session create + reuse | PASS | 270 |
| 2 | sync prompt | PASS | 1904 |
| 3 | SSE stream | PASS | 4036 |
| 4 | built-in tool/plugin invocation | SKIP | 181 |
| 5 | STT on in-script generated WAV | PASS | 10961 |
| 6 | TTS | PASS | 1139 |
| 7 | Media PNG analysis | PASS | 5187 |
| 8 | workflow | PASS | 14036 |
| 9 | session-memory follow-up | PASS | 1439 |
| 10 | latency summary | PASS | — |

### G.3 Austin scene — `after-austin-1440x900.png`

Share-link load `#lat=30.25146&lon=-97.7533&alt=800&heading=0&pitch=-35&v=2&l=a.f.m.s.t&lo=s.c.d&hv=1&hud=tactical`
(30°15'05.26"N 097°45'11.89"W, MGRS 14R PU 1994 4730; every MOVEMENT toggle ON via the layer-state tokens, DENSE
satellites), headless Chromium 1440×900 (ui-validator), 75 s settle (the vessels layer refreshes its scene bbox every
60 s), DATA LAYERS panel expanded, captured 02:06:56Z. Acceptance eval true: all five toggles `active`, no row
contains `UNAVAILABLE` or `HTTP 502`.

| Row | Badge (toggle) | Subtext (verbatim) |
|---|---|---|
| Satellites | `ON` | `LIVE · CelesTrak · 15m ago` |
| Live Flights | `DEGRADED` | `DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed` |
| Military Flights | `ON` | `LIVE · adsb.lol · just now` |
| Live Vessels | `ON` | `Demo replay · No vessels in scene (demo replay covers the Texas Gulf coast)` |
| Street Traffic | `DEGRADED` | `DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds)` |

### G.4 Galveston Bay scene — `after-galveston-1440x900.png`

Share-link load `#lat=29.55&lon=-94.80&alt=12000&heading=0&pitch=-45&v=2&l=a.f.m.s.t&lo=s.c.d&hv=1&hud=tactical`,
same procedure, captured 02:18:42Z. Acceptance eval true. Row counts at capture: `ais-live-vessels:12 flights:689 military:25 satellites:11.5K`.

| Row | Badge (toggle) | Subtext (verbatim) |
|---|---|---|
| Satellites | `ON` | `LIVE · CelesTrak · 27m ago` |
| Live Flights | `DEGRADED` | `DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed` |
| Military Flights | `ON` | `LIVE · adsb.lol · just now` |
| Live Vessels | `DEGRADED` | `DEGRADED · Demo replay · AISSTREAM_API_KEY not set - demo replay, not live AIS` |
| Street Traffic | `DEGRADED` | `DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds)` |

Street Traffic reads the TomTom keyless form on both scenes because the OSM roads DID load (kumi.systems answered
from the sandbox egress); the `DEGRADED · Overpass · <reason>` form is exercised by the unit tests (all-mirror
failure). Live Flights reads `DEGRADED · adsb.lol …` because OpenSky connect-times-out from cloud egress
(`OPENSKY_CLIENT_ID`/`OPENSKY_CLIENT_SECRET` are also unset).

## F / H — see the final response

Commit `chore(closeout): defaulted rename decisions, configurable Overpass mirrors, closeout audit`, its push to
`origin ondemand-serverless` (`mk42-ai/gods-eye-view`), the export zip `ondemand-spatial-closeout-<sha>.zip`, the
patch `ondemand-spatial-closeout-4dc98fc..<sha>.patch` and the `git am --check` result are reported in the run's
final response; they cannot be written into the commit they describe.

## Still blocked / external

- `OPENSKY_CLIENT_ID`, `OPENSKY_CLIENT_SECRET`, `AISSTREAM_API_KEY`, `TOMTOM_API_KEY` are not on the Vercel project
  → Live Flights / Live Vessels / Street Traffic stay DEGRADED (with the key named) on the real deployment.
- Public Overpass mirrors from cloud egress: only `kumi.systems` (and intermittently `private.coffee`) answer;
  `overpass-api.de` and aliases refuse with HTTP 406. A private/self-hosted mirror in `OVERPASS_ENDPOINTS` is the
  durable option.
- The Vercel project's production target is still `BLOCKED` on the Git-author check; the project is Git-linked to
  `mk42-ai/ondemand-eand-spatial` (`main`), not to this repository.
- Graphs MCP and the Chrome DevTools MCP pod were not provisioned in this run; browser verification used the
  sanctioned `ui-validator` (`ui_validate.py`) with in-page Promise settles and a console keepalive to work around
  its post-settle socket-timeout on a busy WebGL page.


## I. Real-deployment closeout addendum (appended 2026-09-19T09:16:33Z; branch tip `13eb0e3`)

Appended in place by the closeout pass. Sources: the deployment-pass report `ondemand-eand-spatial-deployment-pass-2026-09-19.md` (08:46Z–09:04Z), the icon once-over pass (07:52Z–08:36Z), this session's integration probes (07:25Z–07:29Z) and the live checks of this pass (09:12Z–09:14Z). No credential value appears anywhere in this section — names and PRESENT/MISSING only.

### I.1 Real-deployment verification (step 5)

**What step 5 actually produced.** The platform reported `deployable github repo produced no live preview URL` for that turn because it created no new sandbox (the `13eb0e3` sandbox https://sb-19qnr75rdn3l.vercel.run from the icon pass was still live and was re-verified) and — more importantly — **no real Vercel deployment was created**: `/usr/local/bin/vercel` in this workspace is a platform guardrail shim that answers every invocation (`--version`, `link`, `deploy`, `ls`, `env ls`) with exit 126 `vercel: BLOCKED by platform policy — the Vercel CLI is not available in this environment.` (08:52:21Z) and the policy forbids the `npx vercel` / `api.vercel.com` work-arounds. Confirmed outputs of that pass: the gate run at `13eb0e3` (08:51:14Z→08:52:11Z: `npm ci` exit 0, 4,370 tests = 4,369 pass / 0 fail / 1 skipped, build 7.58 s, 9 functions), the baseline badge read of the current preview target, health reads of every reachable deployment, the workflow-id MATCH/MISMATCH probe, content fingerprints of the two previously unconfirmed deployments, and the badge read of the `13eb0e3` sandbox. Not produced: a new deployment id/URL/inspector URL, `vercel ls`/`inspect`/`env` output, a selftest run, a production promotion.

**Deployments (public reads 08:52Z–08:55Z; ids/created/creator from the 05:5xZ audit read):**

| Deployment | id | Created (UTC) | Target | State | Commit carried | How confirmed |
|---|---|---|---|---|---|---|
| `…-bzetw180c-…` | dpl_743G2gRzJbm4Vxm9d1PZQtqJTo7i | 2026-09-18T18:39:07Z | none (brief's "current preview target") | READY | 4dc98fc | client bundle `index-DeEWR6S9.js` / `index-CuCZmWny.css`; emoji-era DATA LAYERS |
| `…-ogowq1fls-…` | dpl_GDb1kCXDZRQzYdS8BzNTt5JvLuQq | 2026-09-19T02:37:44Z | none | READY | **7a24c7a** — `chore(closeout): defaulted rename decisions, configurable Overpass mirrors, closeout audit` (2026-09-19T02:24:35Z) | serves `index-B8CcjpKB.js` / `index-DLeUtkIZ.css` = local `VITE_SERVERLESS_MODE=true` build of 7a24c7a (and of 0677f16 — client-identical); `POST /api/overpass` form body → `400 Exactly one data query is required` (the pre-0677f16 body bug) |
| `…-j8s1kk2zd-…` | dpl_33CfSGEWDxgimgodxnHzJUW3CbwT | 2026-09-19T02:41:20Z | preview target | READY | **0677f16** — `fix(serverless): re-encode Vercel-pre-parsed form bodies so /api/overpass sees data=<query>` (2026-09-19T02:41:15Z); code-identical to the docs-only 5779800 / 18e2a68 | same client bundle; `POST /api/overpass` form body → `400 Overpass query has an unbounded selector` = body parsed ⇒ fix present |
| `…-gk1p1ydur-…` | dpl_7CAoCxdEQRQ7W12mzwz4iGKDK4V9 | — | production | BLOCKED (Git-author check); URL serves a foreign HTML shell for every path | e799775 (not this codebase) | 08:52:53Z |
| `ondemand-eand-spatial.vercel.app`, `…-schoolhack-web-team…`, `…-git-main-…` | — | — | production aliases | `404 DEPLOYMENT_NOT_FOUND` | — | 08:52:54Z |

Commits on `ondemand-serverless` since the last confirmed deployment (none of them deployed to the real project): `7cce6dd` fix(ui) UI/UX sweep + OnDemand integration verification · `fc47066` fix(ui) last two glyph icons + template stroke 1.75 · `1863fa2` fix(ui) DISPLAY/collapse icon colour · `0b2a8d3` test(ui) arrow guard / stroke contract / icon chip · `17b21e5` docs(brand) ICON_SOURCE §3.3 · `ee4aa7c` fix(ui) LOCATION search + TR-3B icon ink · `13eb0e3` test(ui) colour pins (tip, 2026-09-19T08:32:21Z; push ranges 7cce6dd..17b21e5 at 08:20:46Z and 17b21e5..13eb0e3 at 08:32:21Z).

**Health / selftest — baseline vs now.**

| Check | Baseline (G.1/G.2, 01:47Z, sandbox with runtime credentials) | Now |
|---|---|---|
| `/api/ondemand/health` | 200, five `healthy`, `configured:true` | j8s1kk2zd / ogowq1fls / bzetw180c: 200, five `healthy`, `configured:true` (08:52:43Z–08:52:50Z); `flowVersion.source = GODS_EYE_FLOW_VERSION` (`resolvedVia: alias`) on all three; `spatialFlowId.source = ONDEMAND_SPATIAL_WORKFLOW_ID` on j8s1kk2zd/ogowq1fls, `default` on bzetw180c. `/api/health` is not a route (404 `Unknown API route`). |
| selftest | **9 passed / 0 failed / 1 skipped** (step 4 built-in plugin skipped: `ONDEMAND_SPATIAL_AGENT_ID` unset), 38,972 ms; workflow step 14,036 ms; the earlier full execution 163 s with TTFL 657 ms | **not run** — `ONDEMAND_SELFTEST_TOKEN` is absent from this workspace (route answers 404 `not_found` without the `x-selftest-token` header on all three deployments, 08:52Z) |
| workflow execute | success | env-resolved id on j8s1kk2zd → **404 `upstream_error` / `not_found`** (07:25:46Z and 08:56:01Z); explicit `6aace534859f7b0abb53d99a` → **success in 133,562 ms, time-to-first-log 362 ms** (07:26:43Z; 29 log lines across 9 nodes) ⇒ the Vercel value of `ONDEMAND_SPATIAL_WORKFLOW_ID` MISMATCHES the live workflow |
| chat sync / SSE / TTS→STT / media / earthquakes (real preview j8s1kk2zd, 07:25Z) | — | 201 session, 200 `PONG`, 200 SSE (13 `fulfillment` events + 3 heartbeats, 9.03 s), TTS 200 (49,536 bytes) → STT 200 match 0.989, media 200, earthquakes 24 h 200 count 100 / Gulf circle 200 count 3 / unknown param 400 |

**MOVEMENT badge tables — verbatim.**

BEFORE ① — the user's pre-fix capture (`canvas-screenshot-385x525.png`, Austin, emoji-era build):

| Row | Badge | Reason (verbatim) | Source |
|---|---|---|---|
| Satellites | `UNAVAILABLE` | UNAVAILABLE · CelesTrak · CelesTrak unreachable (+ chip `DENSE`) | CelesTrak |
| Live Flights | `UNAVAILABLE` | UNAVAILABLE · OpenSky · OpenSky HTTP 502 · retry 20s | OpenSky |
| Military Flights | `UNAVAILABLE` | UNAVAILABLE · adsb.lol · adsb.lol HTTP 502 | adsb.lol |
| Live Vessels | `UNAVAILABLE` | UNAVAILABLE · AISStream · AIS live relay is unavailable in the serverless deployment | AISStream |
| Street Traffic | `OFF` (cut at the panel fold) | — | TomTom |

BEFORE ② — current preview target `bzetw180c` (4dc98fc), 1440×900, five toggles ON, Austin 08:48:45Z / Galveston Bay 08:50:35Z:

| Row | Austin badge | Austin reason (verbatim) | Galveston badge | Galveston reason (verbatim) |
|---|---|---|---|---|
| Satellites / CelesTrak | `ON` | LIVE · CelesTrak · 1m ago | `ON` | LIVE · CelesTrak · 2m ago |
| Live Flights / OpenSky | `DEGRADED` | DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed | `DEGRADED` | same |
| Military Flights / adsb.lol | `ON` | LIVE · adsb.lol · just now | `ON` | LIVE · adsb.lol · just now |
| Live Vessels / AISStream | `ON` | Demo replay · No vessels in scene (demo replay covers the Texas Gulf coast) | `DEGRADED` | DEGRADED · Demo replay · AISSTREAM_API_KEY not set - demo replay, not live AIS |
| Street Traffic / TomTom | **`UNAVAILABLE`** | UNAVAILABLE · OpenStreetMap · Road data temporarily unavailable (30 × `400 Exactly one data query is required` from `/api/overpass`) | `DEGRADED` | DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds) |

Panel icons on this build: 14 emoji + 7 text glyphs, 0/18 rows with an inline `<svg>`, `↻` ORBIT glyph present; `HTTP 502` strings: 0 on both scenes.

AFTER — `13eb0e3` build on the sandbox https://sb-19qnr75rdn3l.vercel.run (keyless; **not a real-project deployment** — none exists), Austin 08:57:27Z / Galveston Bay 08:58:50Z:

| Row | Austin badge | Austin reason (verbatim) | Galveston badge | Galveston reason (verbatim) |
|---|---|---|---|---|
| Satellites / CelesTrak | `ON` | LIVE · CelesTrak · 33m ago | `ON` | LIVE · CelesTrak · 34m ago |
| Live Flights / OpenSky | `DEGRADED` | DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed | `DEGRADED` | same |
| Military Flights / adsb.lol | `ON` | LIVE · adsb.lol · 14s ago | `ON` | LIVE · adsb.lol · just now |
| Live Vessels / AISStream | `ON` | Demo replay · No vessels in scene (demo replay covers the Texas Gulf coast) | `DEGRADED` | DEGRADED · Demo replay · AISSTREAM_API_KEY not set - demo replay, not live AIS |
| Street Traffic / TomTom | `LOADING` (at 60 s) | DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds) · SIMULATED — add TomTom key for live | `DEGRADED` | DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds) |

0 UNAVAILABLE, 0 `HTTP 502`, DATA LAYERS emoji 0 / glyphs 0, 18/18 rows with `svg[data-icon aria-hidden="true"]` at 18×18 / stroke 1.75, 0 black icons, 0 U+21BB / U+21C4. Open honesty items (not icon defects): Austin Vessels `ON` over the demo replay (F-06); Austin Street Traffic `LOADING` badge beside a DEGRADED reason during the Overpass mirror rotation.

**Production target:** NOT promoted. Reason: promotion was gated on a passing real preview deployment of `13eb0e3`, and no deployment could be created from this workspace (CLI shim). Current state: production aliases `404 DEPLOYMENT_NOT_FOUND`; dpl_7CAoCxdEQRQ7W12mzwz4iGKDK4V9 BLOCKED (Git-author check) and serving a foreign application shell.

### I.2 Env-var table (PRESENT / MISSING only; preview target j8s1kk2zd runtime evidence 08:52Z–08:53Z; production has no reachable runtime)

| Name | Preview | Production | Evidence / required action |
|---|---|---|---|
| ONDEMAND_API_KEY | PRESENT | audit 05:51Z: PRESENT | health `configured:true`, chat 200 |
| ONDEMAND_SPATIAL_WORKFLOW_ID (env id lTU2obzhz3oJXEMI) | PRESENT — **value MISMATCH** | audit: PRESENT | **set to the 24-hex `6aace534859f7b0abb53d99a` (or delete so the code default applies)** |
| ONDEMAND_SPATIAL_FLOW_VERSION | PRESENT | audit: PRESENT | canonical name; keep |
| GODS_EYE_FLOW_VERSION (legacy alias, env id usC3wgbut65gTkaR) | PRESENT | audit: PRESENT | **REMOVE from production, preview and development** once ONDEMAND_SPATIAL_FLOW_VERSION carries the same value — `server/ondemand/config.js` `FLOW_VERSION_ENV.order = ['GODS_EYE_FLOW_VERSION', 'ONDEMAND_SPATIAL_FLOW_VERSION', default '1']` checks the alias FIRST, so health keeps reporting `source: GODS_EYE_FLOW_VERSION` until the alias is gone; no code change needed (`vercel env rm GODS_EYE_FLOW_VERSION <target> --yes`, then redeploy and expect `flowVersion.source = ONDEMAND_SPATIAL_FLOW_VERSION`) |
| OPENSKY_CLIENT_ID / OPENSKY_CLIENT_SECRET | MISSING | MISSING | `x-opensky-auth-mode-used: adsblol-regional`, reason `opensky_unreachable_regional_fallback`; add both (OAuth2 client credentials) |
| AISSTREAM_API_KEY | MISSING | MISSING | `X-Provider-Error: AISSTREAM_API_KEY not set - demo replay, not live AIS` |
| TOMTOM_API_KEY | MISSING | MISSING | `TOMTOM_API_KEY not set - flow colours are simulated on live OSM roads` |
| CelesTrak / adsb.lol keys | n/a (keyless upstreams; both LIVE) | n/a | none required |
| OVERPASS_ENDPOINTS / OVERPASS_UPSTREAMS / ROAD_NETWORK_SOURCE | MISSING | MISSING | `road_network_status.fromEnv` all false — set once the Overpass decision (I.3) is made |
| KV_REST_API_URL / KV_REST_API_TOKEN / UPSTASH_REDIS_REST_* | not observable (no AISStream key ⇒ collector never touches KV); absent in the 05:51Z name read | — | optional; only with AISSTREAM_API_KEY |
| ONDEMAND_SELFTEST_TOKEN | PRESENT on Vercel (health names); **MISSING in this workspace** | audit: PRESENT | needed here to run the selftest |

### I.3 Recommendation — the open Overpass decision (and the other three upstreams)

**Overpass (Street Traffic road geometry).** Measured 2026-09-19T07:0xZ from this workspace: `overpass-api.de`, `lz4.` and `z.` answer **HTTP 406** to every request whose User-Agent contains `gods-eye-view` (the upstream project's identifier that PROVIDER_USER_AGENT still carries) or `example.com`, while neutral UAs get 200/504 — i.e. a UA-keyed refusal, not a query problem; `overpass.kumi.systems` answered 200 in 9.7 s once and timed out (>25–30 s) twice; `private.coffee` timed out. Community evidence: the public main instance has been unreliable through 2026, private.coffee mirrors carried stale `timestamp_osm_base` values (2026-01-15 / 2026-01-06), and there is an April-2026 discussion (pnorman) of requiring OSM sign-in / API keys for the public instance — https://community.openstreetmap.org/t/overpass-api-performance-issues/140598 , https://community.openstreetmap.org/t/overpass-api-performance-issues/140598?page=4 , https://community.openstreetmap.org/t/overpass-api-performance-issues/140598?page=8 . Alternatives on the table: the paid Geofabrik Overpass service, scope-limited instances, self-hosting (the MMD fork is faster with meta), FairwayMapper pooled slots (require 20 OSM changesets in 90 days), and server-side caching of bbox queries (schrankmonster measured ~28 s for an uncached bbox query — https://www.schrankmonster.de/2025/10/29/openstreetmap-overpass-but-cached/ ). **Recommendation:** a cached proxy in front of Overpass (edge `s-maxage` + the repo's 24 h memory / 7 d disk tiers already exist for local runs — a KV-backed tier is the serverless equivalent), an explicit per-mirror timeout (`OVERPASS_MIRROR_TIMEOUT_MS`, default 12 s) and a fallback mirror list (`OVERPASS_ENDPOINTS`), with self-hosting or Geofabrik as the paid option when the layer is load-bearing; whether to change the User-Agent identity that overpass-api.de refuses is part of the same decision. **Status: decision pending human** (the code already reads `OVERPASS_ENDPOINTS` / `ROAD_NETWORK_SOURCE=off`).

**OpenSky (Live Flights).** REST root `https://opensky-network.org/api`, `GET /states/all` with `lamin/lomin/lamax/lomax`; anonymous calls are rate-limited and authenticated access now uses OAuth2 client-credentials tokens — https://openskynetwork.github.io/opensky-api/rest.html . The proxy already requests OAuth (`x-opensky-auth-mode-requested: oauth`) and falls back to adsb.lol; the `HTTP 502 · retry 20s` seen on the pre-fix build was the raw upstream failure surfaced before the 2026-09-18 breaker/fallback landed. From Vercel egress the TCP connect itself times out, so credentials alone will not fix it; keep the regional fallback and retry with auth + backoff (`OPENSKY_CLIENT_ID` / `OPENSKY_CLIENT_SECRET`).

**AISStream (Live Vessels).** WebSocket `wss://stream.aisstream.io/v0/stream` with `APIKey` + `BoundingBoxes`; open issue #15 (Mar 2026) reports subscriptions accepted with zero messages delivered — https://github.com/aisstream/aisstream/issues/15 , https://dev.to/jj/follow-that-ship-with-aisstreamer-21g3 . A WebSocket relay cannot run inside a Vercel serverless function; the request-driven bounded collector (`server/providers/vessels/ais-serverless.js`) plus the labelled demo replay is the serverless posture — keep the DEGRADED / "not live AIS" wording (and the previous 'unavailable in serverless' text is retired) unless an external relay is added.

**TomTom (Street Traffic flow).** Traffic API (Flow + Incidents + raster/vector tiles) refreshed every 30 s, free tier available — https://www.tomtom.com/discover/explore-the-tomtom-traffic-api/ , https://www.tomtom.com/products/traffic-apis/ . Setting `TOMTOM_API_KEY` turns the simulated flow into live speeds; roads still come from Overpass (I.3 above).

### I.4 Icon once-over — surfaces outside DATA LAYERS (1440×900, Austin + Galveston Bay, 08:06Z–08:31Z)

| Surface | Before (7cce6dd) | After (13eb0e3) |
|---|---|---|
| ORBIT indicator | `↻` U+21BB text glyph | `rotate-ccw` inline icon (13 px, 1.75) |
| Directions SWAP chip | `⇄` U+21C4 text glyph | `move-horizontal` inline icon (12 px, aria-label "Swap A and B") |
| Presets · DISPLAY toggles · LOCATION tray · share · collapse buttons · radio transports · TR-3B (33 template icons) | `stroke-width="2"` | `stroke-width="1.75"` |
| DISPLAY toggles (6) · LOCATION search · TR-3B | computed `rgb(0,0,0)` (UA buttontext — invisible on the dark glass) | `rgba(243,243,243,.55)` idle / accent active; collapse icons `rgba(243,243,243,.68)`; TR-3B `rgba(190,220,255,.95)` |
| Whole document | 89 `svg[data-icon]` (62 × 1.75, 27 × 2), 2 glyphs, 8 black icons | **91 `svg[data-icon]`, all 1.75, 0 emoji / 0 glyphs, 0 black icons, 0 wraps / 0 clips** |

Gates after the fixes: format 966 files, boundaries 109 groups, emoji guard 9/9, unit 4,369/4,370 (1 skip), ondemand 219, serverless 58, build OK, 9 functions. `ui_validate.py` exits 2 (`TimeoutError: timed out`) on the Cesium page, so the repo Puppeteer harness (setsid, group-kill, wall-clock cap) was used; screenshots `icons-before-austin-*.png`, `icons-after-austin-*.png`, `icons-before-galveston-*.png`, `icons-after-galveston-*.png` (icon pass) and `baseline-*.png` / `after-sandbox13eb0e3-*.png` (deployment pass).

### I.5 OnDemand registration status (step (a) of the closeout pass)

- **Live public API docs** (`GET <gateway>/config/v1/public/docs/categories`, HTTP 200 at 2026-09-19T09:13:10Z — a plain-UA request is refused by Cloudflare error 1010, a browser UA is accepted): the documented services are Media API, Services API, MQTT User Management API, **Projects Management API** (new since the 2026-09-18 pack: create/list/get/update/delete project, sessions by project), Agents Flow Builder API (workflows, executions, approval gates) and Chat API. **No agent, plugin, REST-agent or skill creation operation is documented** — the step-3 answer in `dashboard-registration-pack.md` §1 ("dashboard-only") stands. The agent runtime's platform tool `suggest_plugins` answered `{"message":"Invalid API Key","errorCode":"unauthenticated"}` at 09:13Z, so no plugin lookup or creation happened through that channel either.
- **tool ID = null · agent ID = null.**
- **One test call of the tool endpoint the REST agent will front** (https://ondemand-eand-spatial-j8s1kk2zd-schoolhack-web-team.vercel.app/api/sources/earthquakes — preview deployment of 0677f16; the earthquakes route is unchanged at 13eb0e3): 09:14:15Z Galveston Bay bbox (minlatitude 28.8 / maxlatitude 30.3 / minlongitude −95.6 / maxlongitude −94.1, 2026-08-20→2026-09-19) → **HTTP 200, 0 events, 0.558 s**; 09:14:16Z Austin 500 km circle, 7 days → **HTTP 200, 81 events, 0.298 s** (first event `tx2026slpvpi`, M1.2, 38 km ENE of McKinney Acres, Texas). The ephemeral sandbox https://sb-19qnr75rdn3l.vercel.run also answered 200 (09:12:37Z) but must not be used in `servers` (it expires).
- **Dashboard click-path** (values from `dashboard-registration-pack.md` §2–§5; `servers[0].url` substituted): (1) app.on-demand.io → My Agents → **Create Agent** → **Rest Api Agent**; (2) name `earthquake_search` (or `OnDemand Spatial — earthquake_search`), description from `docs/ondemand-workflows/tools/earthquake_search.openapi.json` `info.description`; (3) paste that OpenAPI 3.0.3 document with `servers` set to `[{"url": "https://ondemand-eand-spatial-j8s1kk2zd-schoolhack-web-team.vercel.app"}]` (replace with the production URL once one exists); (4) authentication: none (the route is public, rate-limited server-side); parameters: the 14 optional query parameters (`starttime`, `endtime`, `minmagnitude`, `maxmagnitude`, `latitude`, `longitude`, `maxradiuskm`, `minlatitude`, `maxlatitude`, `minlongitude`, `maxlongitude`, `limit`, `orderby`, `mode`) — leave all optional; (5) Test with `latitude=30.25146&longitude=-97.7533&maxradiuskm=500` (expect HTTP 200 and a `count`); (6) Save → note the agent id; (7) My Agents → **Create Agent** → **OnDemand Spatial** (system prompt from pack §2 "System behaviour / prompt"), attach the REST agent, then create and attach the nine skills of pack §4; (8) Subscribe / add to conversation, then set `ONDEMAND_SPATIAL_AGENT_ID` in Vercel so selftest step 4 stops skipping; (9) wire the workflow: pack §5 (D)/(E) — read the workflow's trigger from the Flow Builder (workflow `6aace534859f7b0abb53d99a`, the value `ONDEMAND_SPATIAL_WORKFLOW_ID` must hold) and optionally attach earthquake_search to its LLM nodes.


## Reapply 2026-09-19 (appended 2026-09-19T10:21:48Z)

The `13eb0e3` sandbox had expired (HTTP 410), so the tip was rebuilt and redeployed to a fresh sandbox. No credential value appears here.

| Item | Value |
|---|---|
| Source sync | 10:15:53Z `git fetch origin && git checkout ondemand-serverless && git pull --ff-only` → exit 0/0/0, `Already up to date`; HEAD `8155786df6e42765a2685e8ef1dbc6c591a72ba4`; drift `13eb0e3..8155786` = 1 commit `8155786 docs(audit): real-deployment closeout 2026-09-19` (09:16:55Z) |
| Install | `PUPPETEER_SKIP_DOWNLOAD=1 npm ci` exit 0 (10:16:27Z→10:16:32Z, lockfile unchanged) |
| Gates (run in parallel 10:16:32Z→10:17:05Z) | format ✓ 966 files · boundaries ✓ 109 export groups / 762 modules · emoji/glyph guard ✓ 9/9 · unit ✓ 4,370 = 4,369 pass / 0 fail / 1 skipped · ondemand ✓ 219/219 · serverless ✓ 58/58 · build ✓ 13.78 s · serverless functions **9** (≤ 12) |
| New sandbox | `sbx_tn5n0Dt8LpYD9TiC2weq5xGceyAZ`, node 24.14.1, created 2026-09-19T10:17:35Z, **expires 2026-09-19T11:47:35Z** (90 min); `npm ci` ✓, `vite build` 5.58 s ✓, 9 functions; **https://sb-1np4tjtbq20v.vercel.run** HTTP 200 at 10:18:07Z (keyless: `.env` = SERVERLESS_MODE, VITE_SERVERLESS_MODE, PORT, HOST only) |
| `/api/health` | 404 `{"error":"Unknown API route"}` — not a route of this app |
| `/api/ondemand/health?envNames=1` (10:18:28Z) | 200; `ondemand/chat/speech/media/workflow: not configured`, `configured:false`; `flowVersion.source = default` (`resolvedVia: default`, canonical ONDEMAND_SPATIAL_FLOW_VERSION, alias GODS_EYE_FLOW_VERSION — neither set on the sandbox); `spatialFlowId.source = default` (canonical ONDEMAND_SPATIAL_WORKFLOW_ID); `env.names = [SERVERLESS_MODE, VITE_SERVERLESS_MODE]` |
| Selftest | **not run** — `ONDEMAND_SELFTEST_TOKEN` (and every OnDemand credential) is absent from this workspace and from the keyless sandbox; the route answers `404 {"error":"not_found"}` without the `x-selftest-token` header (10:18:28Z). Last credentialed result remains G.2 (9 passed / 0 failed / 1 skipped, 01:47:41Z) |

### Headless sweep (repo Puppeteer harness, 1440×900, five MOVEMENT toggles ON + Directions/Radio/Space Missions/Earthquakes/Global Context, every panel expanded, 55 s settle)

| Assertion | Austin (MGRS 14R PU 1994 4730) 2026-09-19T10:20:37.776Z | Galveston Bay 2026-09-19T10:20:42.029Z | Target |
|---|---|---|---|
| emoji in DATA LAYERS (text / innerHTML) · document | 0 / 0 · 0 | 0 / 0 · 0 | 0 |
| U+21BB / U+21C4 (text + markup) | 0 / 0 | 0 / 0 | 0 |
| `svg[data-icon]` count | 90 (all stroke-width ['1.75']) | 90 (all stroke-width ['1.75']) | 91 — the 91st in the icon pass was the ASK ONDEMAND overlay close icon, mounted only when that surface is opened |
| DATA LAYERS rows with inline `svg[data-icon aria-hidden]` at 18 px / 1.75 | 18/18 | 18/18 | 18/18 |
| icons computed `rgb(0,0,0)` | 0 | 0 | 0 |
| `HTTP 502` strings / `UNAVAILABLE` badges | 0 / 0 | 0 / 0 | 0 / 0 |
| console errors / page errors | 3 (resource 404 `/api/setup/status`, 503 `/api/overpass`, `/api/firms`) / 0 | 1 (404 `/api/setup/status`) / 0 | — |

Austin — 18 rows verbatim:

| # | Row | Badge | Reason / subtext |
|---|---|---|---|
| 1 | Satellites | `ON` | LIVE · CelesTrak · 1m ago |
| 2 | Live Flights | `DEGRADED` | DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed |
| 3 | Military Flights | `DEGRADED` | DEGRADED · adsb.fi · adsb.lol rate limited (retry in 30s) - adsb.fi feed |
| 4 | Live Vessels | `ON` | Demo replay · No vessels in scene (demo replay covers the Texas Gulf coast) |
| 5 | Street Traffic | `DEGRADED` | DEGRADED · Overpass · all 5 mirrors failed - last: z.overpass-api.de HTTP 406 |
| 6 | Transit | `OFF` | GTFS-RT · never |
| 7 | Bike Share | `OFF` | GBFS · never |
| 8 | Cameras | `OFF` | CCTV + Street View fallback · never |
| 9 | Mapped ALPR Cameras | `OFF` | OpenStreetMap · community mapped · never |
| 10 | Mapped Installations | `OFF` | OpenStreetMap + optional Google Maps Places · never |
| 11 | Data Centers | `OFF` | Local · never |
| 12 | Submarine Cables | `OFF` | TeleGeography · never |
| 13 | Dams | `OFF` | USACE · never |
| 14 | Space Missions (30d) | `ON` | Launch Library 2 · 50s ago |
| 15 | Earthquakes (24h) | `ON` | USGS · 11s ago |
| 16 | Active Fires | `OFF` | NASA FIRMS · LIVE · never |
| 17 | Directions | `ON` | OSM routing · SET A, then click the globe |
| 18 | Radio | `ON` | Radio Browser · 1m ago |

Galveston Bay — 18 rows verbatim:

| # | Row | Badge | Reason / subtext |
|---|---|---|---|
| 1 | Satellites | `ON` | LIVE · CelesTrak · 1m ago |
| 2 | Live Flights | `DEGRADED` | DEGRADED · adsb.lol · OpenSky unreachable from this deployment (connect timeout) - adsb.lol regional feed |
| 3 | Military Flights | `DEGRADED` | DEGRADED · adsb.fi · adsb.lol rate limited (retry in 30s) - adsb.fi feed |
| 4 | Live Vessels | `DEGRADED` | DEGRADED · Demo replay · AISSTREAM_API_KEY not set - demo replay, not live AIS |
| 5 | Street Traffic | `DEGRADED` | DEGRADED · TomTom · TOMTOM_API_KEY not set — showing simulated flow on live OSM roads (set TOMTOM_API_KEY in Vercel for live speeds) |
| 6 | Transit | `OFF` | GTFS-RT · never |
| 7 | Bike Share | `OFF` | GBFS · never |
| 8 | Cameras | `OFF` | CCTV + Street View fallback · never |
| 9 | Mapped ALPR Cameras | `OFF` | OpenStreetMap · community mapped · never |
| 10 | Mapped Installations | `OFF` | OpenStreetMap + optional Google Maps Places · never |
| 11 | Data Centers | `OFF` | Local · never |
| 12 | Submarine Cables | `OFF` | TeleGeography · never |
| 13 | Dams | `OFF` | USACE · never |
| 14 | Space Missions (30d) | `ON` | Launch Library 2 · 50s ago |
| 15 | Earthquakes (24h) | `ON` | USGS · 14s ago |
| 16 | Active Fires | `OFF` | NASA FIRMS · LIVE · never |
| 17 | Directions | `ON` | OSM routing · SET A, then click the globe |
| 18 | Radio | `ON` | Radio Browser · 1m ago |

Screenshots: `reapply-austin-1440x900-full.png`, `reapply-austin-1440x900-data-layers.png`, `reapply-austin-1440x900-display.png`, `reapply-galveston-1440x900-full.png`, `reapply-galveston-1440x900-data-layers.png`, `reapply-galveston-1440x900-display.png`.
