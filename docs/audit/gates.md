# Gate ledger — God's Eye View × OnDemand serverless conversion

Maintained on branch `ondemand-serverless` (upstream base `0d41b6be5490db1f10a171f238be75db4d4ec3b4`, 2026-09-16).
Every status below is backed by a file in this repository; nothing is marked EXITED without a real run.

| Gate       | Scope                                                                                                                                        | Status                                                                                              | Evidence                                                                                                                                                |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Gate 0** | Read-only audit of the audited commit (file tree, verbatim snapshot, size listing, AI-provider + data-provider hits, server route inventory) | **EXITED** — 2026-09-17T06:41Z                                                                      | `docs/audit/file-tree.txt`, `gate0-snapshot.md`, `size-listing.txt`, `ai-provider-hits.txt`, `provider-hits.txt`, `server-routes.md` (commit `2fc84a3`) |
| **Gate 1** | Serverless conversion + same-origin OnDemand proxy, with a LIVE 10-step OnDemand contract run                                                | **EXITED** — 2026-09-18T04:51Z (live run with a user-supplied key: 8 passed / 0 failed / 2 skipped) | `docs/ONDEMAND_API_CURRENT.md` §17, `docs/ondemand-workflows/contract-baseline.json`, `docs/audit/deployment-verification.md`                           |
| Gate 2     | —                                                                                                                                            | NOT ENTERED                                                                                         | —                                                                                                                                                       |
| Gate 3     | Data-source plugin rows (starts with the Seismic row: USGS `earthquake.search`; then the maritime.live row that lifts the ais-live 501)      | NOT ENTERED                                                                                         | —                                                                                                                                                       |
| Gate 4     | —                                                                                                                                            | NOT ENTERED                                                                                         | —                                                                                                                                                       |
| Gate 5     | —                                                                                                                                            | NOT ENTERED                                                                                         | —                                                                                                                                                       |
| **Gate 6** | Principal OnDemand workflow (`GodsEye Advanced Spatial Workflow` v1) created + activated through the documented Flow Builder API, verified live, wired as the selftest step-8 default; agent + nine skills packaged for dashboard registration (no documented create API) | **EXITED (workflow) / DASHBOARD-PENDING (agent + skills)** — 2026-09-18T07:33Z (workflow id `6aace534859f7b0abb53d99a`; selftest 9 passed / 0 failed / 1 skipped) | `docs/ondemand-workflows/README.md`, `gods-eye-advanced-v1.json`, `verification-2026-09-18.json`, `contract-baseline.json`, `docs/audit/deployment-verification.md` §8, `docs/audit/dashboard-registration-pack.md`, `docs/ondemand-skills/` |
| Gate 7     | —                                                                                                                                            | NOT ENTERED                                                                                         | —                                                                                                                                                       |
| Gate 8     | —                                                                                                                                            | NOT ENTERED                                                                                         | —                                                                                                                                                       |

## Gate 1 — exit record (2026-09-18)

**EXITED at 2026-09-18T04:51:41Z.** The user supplied an OnDemand API key for this run (masked everywhere as `<redacted>`, held only in the process environment). With it:

- Credential validation: `POST /chat/v1/sessions` → **201** in 249 ms at 04:45:10.744Z (`sessionId 6aacc1d6ab2c7f21f3232d03`) — key classification **ondemand**.
- Live 10-step contract run (`scripts/ondemand-contract-test.mjs`, direct mode, `endpointId predefined-gpt-5.6-luna`, session `6aacc33eb9401965d65923e0`): session create+reuse 200 · sync 200 · SSE stream 200 (ttfd 1,277 ms, 29 deltas, `[DONE]`) · tool invocation **SKIP** (this account has 0 agents — `GET /plugin/v1/list total=0`) · STT on an in-script WAV 200 · TTS 200 (42,624 MP3 bytes) · Media PNG analysis 200 (`actionStatus completed`) · workflow **SKIP by design** (`ONDEMAND_SPATIAL_FLOW_ID` unset) · session-memory follow-up 200 · latency summary. `CONTRACT RESULT: passed=8 failed=0 skipped=2 totalMs=31012`, exit 0.
- Live discovery: 23 active endpoint ids (`GET /config/v1/public/endpoints`), 13 predefined + 1 user-defined reasoning modes (`GET /config/v1/public/reasoning_modes`), tier mapping ASK/INVESTIGATE/DEEP chosen from real ids — all in `docs/ONDEMAND_API_CURRENT.md` §17.

Residual carried forward (not an exit blocker under the Gate 1 live-exit definition): the branch has still not been observed on a real Vercel function runtime — the 2026-09-17 record below explains why (no God's Eye deployment exists; CLI blocked in the execution environment).

### History — 2026-09-17: why the gate was EXIT BLOCKED before the live key arrived

On 2026-09-17 neither a function-runtime observation nor a live contract run was possible:

1. **No God's Eye deployment exists.** The Vercel account (user `navnit-5790`, team `schoolhack-web-team`) has no project linked to `bilawalsidhu/gods-eye-view` and none named `gods-eye-view` / `gods-eye-view-ondemand-serverless` (project list inspected: `ondemand-hq`, `airev-ondemand-deck`, `ondemand-agent`, `rilian-airev-ondemand-deck`, `ondemand-eand-spatial`, `ondemand-eand-spatial-jyxd`, `airev-ondemand-investor-deck`, `on-demand-ai-investments`).
2. **The fallback host is a different application.** Per the resolution rule, `finalProductionUrl` fell to the READY preview alias `https://ondemand-eand-spatial-opal.vercel.app` (project `ondemand-eand-spatial`, `prj_VbHbEhFSDFkdXCqlq8XqONoFWQHO`, deployment `dpl_FKoHEVctqpLzNMaHV9sSRdmzcrw6`, commit `b8cdcf50` of `mk42-ai/ondemand-eand-spatial`) because the production target (`dpl_7CAoCxdEQRQ7W12mzwz4iGKDK4V9`, commit `e799775a`) is **BLOCKED** and its alias answers `404 DEPLOYMENT_NOT_FOUND`. That host serves another Vite app; `GET /api/ondemand/health` there returns Vercel's platform `404 NOT_FOUND` (`x-vercel-error: NOT_FOUND`) at 2026-09-17T08:12:13Z — it is **not** the God's Eye serverless build.
3. **A deployment could not be created from this workspace.** `vercel link` / `vercel deploy` / `vercel env pull` are refused by the platform guardrail (`vercel: BLOCKED by platform policy — the Vercel CLI is not available in this environment`, exit 126, 2026-09-17T08:12:26Z) and workarounds are prohibited.
4. **Contract test — neither mode could run live.** `env-pull` mode: refused as above (and `ONDEMAND_API_KEY` is a `sensitive` variable, which Vercel never returns via pull). `proxy` mode: forced, but `finalProductionUrl/api/ondemand/*` does not exist (404), so step 1 fails and steps 2–9 are dependency-skipped — recorded verbatim in `contract-baseline.json` (`mode: "proxy"`).

The branch itself is green (4,144 upstream unit tests, 30 serverless + 93 ondemand tests, boundaries, prettier, production build) and its runtime behaviour is proven only in an ephemeral sandbox emulator (`docs/audit/deployment-verification.md` §1–3), which does not satisfy the Gate 1 exit criterion.

### Gate 1 exit requirements — status after 2026-09-18

- ✅ Live contract run: done 2026-09-18 in direct mode with the user key (see exit record above).
- ⏳ Remaining residual: deploy `ondemand-serverless` to a Vercel project (see `DEPLOYMENT_REPORT` hand-off, Option A/B) and re-run `node scripts/ondemand-contract-test.mjs --mode proxy --proxy-base <url>/api/ondemand --json-out docs/ondemand-workflows/contract-baseline.json` to observe the same 10 steps through the deployed proxy.
- Health must read `configured:true` with chat/media/workflow `healthy` (speech `degraded` is by design — no read-only Services probe is documented).

### Gate 1 residuals (carried into Gate 2/3 regardless of exit)

- (i) **ID variables pending real creation**: `ONDEMAND_SPATIAL_AGENT_ID`, `ONDEMAND_SPATIAL_FLOW_ID`, `ONDEMAND_REASONING_ENDPOINT_ID`, `ONDEMAND_FULFILLMENT_ENDPOINT_ID`, `GODS_EYE_FLOW_VERSION`. Notes: the proxy accepts the target project's existing `ONDEMAND_ENDPOINT_ID` / `ONDEMAND_API_BASE` / `ONDEMAND_KNOWLEDGE_PLUGIN_IDS` as aliases (commit `9026c8c`); `ONDEMAND_REASONING_ENDPOINT_ID` and `GODS_EYE_FLOW_VERSION` have no documented OnDemand concept behind them (`docs/ONDEMAND_PROXY_DESIGN.md` §5) — creating them is a product decision, not a code gap.
- (ii) **`/api/ais-live` answers 501** in serverless mode until the maritime.live plugin row lands in Gate 3 (the AISStream WebSocket relay cannot run in a Vercel Function — `docs/SERVERLESS_LIMITATIONS.md` §3).
- (iii) **Gate 3 starts with the Seismic row** (USGS `earthquake.search`).

## Gate 6 — record (2026-09-18)

**Workflow — done through the API (documented surfaces only).** `POST https://api.on-demand.io/automation/api/workflow/` → 201 `{"id":"6aace534859f7b0abb53d99a"}` at 07:16:04.335Z (a first body carrying a documented `inputText` node was rejected at 07:15:04.217Z with 400 `input: text config missing` — the text configuration is undocumented and was not invented); `POST /workflow/{id}/activate` → 200 at 07:16:14.101Z; `POST /workflow/{id}/execute` → 200 at 07:16:26.986Z (`executionID 6aace54bbb6a9a7035f431fc`), polled to `success` in 163,104 ms with time-to-first-log 657 ms; `GET /execution/{id}/node/outputs` → a StructuredResponse with exactly the seven keys and six actions from the 28-name MapAction set. Export = the documented `GET /workflow/{id}` object (no "Get Code" endpoint exists, §7.3). Version label 1 (`GODS_EYE_FLOW_VERSION` — no version field exists in the API).

**Selftest.** Sandbox `sbx_DV7Jx2deyh9edv9XkSztio1dws7P` (`sb-765981cquksp.vercel.run`), runtime env `ONDEMAND_SPATIAL_FLOW_ID=6aace534859f7b0abb53d99a`, `GODS_EYE_FLOW_VERSION=1`: `GET /api/ondemand/selftest` → **9 passed / 0 failed / 1 skipped** twice (07:32:20Z, 07:33:27Z); step 8 now executes the workflow (time-to-first-log 298 / 276 ms); step 4 remains the only skip (the account has no agents — `GET /plugin/v1/list` total 0 at 07:09:37Z).

**Agent + nine skills — dashboard only.** No create endpoint is documented for agents (§8) or skills (§9), so nothing was invented: `docs/audit/dashboard-registration-pack.md` (agent prompt, `earthquake_search` tool JSON + OpenAPI, click-by-click steps, id paste table) and `docs/ondemand-skills/*.md` (nine SKILL.md bodies) are ready to paste; `src/registry/capabilities.json` carries the `ondemand.agent.pluginId` / `ondemand.skills[].skillId` slots (null until registered).

**Residuals.** (i) attach the `earthquake_search` REST agent to the workflow's `capability_resolver`/`planner` nodes once it exists (`llm.plugins[{id}]`, then `node scripts/ondemand-workflow.mjs update <id>`); (ii) the webhook trigger URL is dashboard-only, so payload-driven runs are not yet exercised from the app; (iii) DEEP (`reasoningMode: high`) is not expressible on an `llm` node.

## 2026-09-18T09:30Z — emulator verification, fork/PR, row 2 scaffold

- **Git unblock:** fork `mk42-ai/gods-eye-view` created (`POST /repos/bilawalsidhu/gods-eye-view/forks` → 202, 09:18:59Z), `ondemand-serverless` pushed to the fork, PR **#657** `mk42-ai:ondemand-serverless → bilawalsidhu:main` opened at 09:20:00Z (https://github.com/bilawalsidhu/gods-eye-view/pull/657).
- **Deployment:** still emulator-only — `docs/audit/deployment-verification.md` §9 (why, and the operator-side `scripts/vercel-file-deploy.mjs`). The credential supplied as `VERCEL_TOKEN` was the OnDemand API key (not transmitted anywhere but OnDemand).
- **Selftest (emulator, 09:29:31Z):** 9 passed / 0 failed / 1 skipped; SSE ttfd 1,783 ms; workflow ttfl 259 ms — `docs/ondemand-workflows/contract-baseline.emulator.json`.
- **Gate 3 row 2 (fires.search, NASA FIRMS / LANCE):** scaffold committed (`881e5a3`) — adapter, `/api/sources/fires` via the catch-all, tool JSON, registry row `registered-unverified`, 13 tests; live path on the emulator = `503 not_configured` (no `NASA_FIRMS_MAP_KEY`). Row 1 closure pack: `docs/audit/gate3-row1-registration.md`.
