# RUN REPORT — OnDemand Spatial × OnDemand, branch `ondemand-serverless`

Refreshed 2026-09-18T09:46Z (worktree = commit `06159d8` + this report's docs commit). Base commit `0d41b6be`. This is the single-page status of the migration; every claim points at a file in this repository. No secret value appears in this report or anywhere in the tree — only environment-variable **names** and Vercel env-var **ids**.

## Success criteria

| Criterion | Last pass (earlier report) | Now (2026-09-18T09:46Z) | Evidence |
|---|---|---|---|
| Regenerated patch / zip linked | exported after Gate 6 (`gods-eye-view-ondemand-serverless-gate6.zip`, `…-gate6.patch`) | **Exported this run:** `ondemand-serverless.patch` (`git diff 0d41b6be..HEAD`) and `gods-eye-view-ondemand-serverless.zip` (`git archive HEAD`, no node_modules/dist/.env*/GIFs), plus root copies of the audit set (see the run response's file table with byte sizes and SHA-256) | workspace output directory of this run |
| PR / push status | push to `bilawalsidhu/gods-eye-view` → **403** (mk42-ai has no write access); no fork | **Fork exists and PR is open:** `mk42-ai/gods-eye-view` was forked at 2026-09-18T09:18:59Z (`POST /repos/bilawalsidhu/gods-eye-view/forks` → 202), the branch was pushed to the fork, and **PR #657** `mk42-ai:ondemand-serverless → bilawalsidhu:main` is open — https://github.com/bilawalsidhu/gods-eye-view/pull/657 (head `06159d8`, 29 commits). Direct push to `bilawalsidhu` is still 403 by design (no write access). This report's commit is **not pushed** (this run is docs/export only, push disabled by instruction) — the PR lags HEAD by exactly this docs commit. | `docs/audit/gates.md` (2026-09-18T09:30Z entry); GitHub PR #657 |
| Real Vercel function-runtime deploy | not achieved — `vercel` CLI is a shim (exit 126) in the sandbox; branch has 0 Vercel deployments; project env fully provisioned incl. 4 new ids | **Still not achieved.** Plugin-verified evidence (Step 5): project `ondemand-eand-spatial` (`prj_VbHbEhFSDFkdXCqlq8XqONoFWQHO`, team `schoolhack-web-team`), latest READY `dpl_FKoHEVctqpLzNMaHV9sSRdmzcrw6` is the exec-brief app (branch `feat/exec-brief-v1.4-b7c0d3d`, alias `ondemand-eand-spatial-opal.vercel.app`), production `dpl_7CAoCxdEQRQ7W12mzwz4iGKDK4V9` BLOCKED, 9 git-source deployments BLOCKED ("couldn't find a Git account for the commit author"), **0 deployments of `ondemand-serverless`**; 19 env vars incl. the 4 created this pass (`GODS_EYE_FLOW_VERSION` usC3wgbut65gTkaR, `ONDEMAND_REASONING_ENDPOINT_ID` I0TXIH0rgSDwzEJB, `ONDEMAND_FULFILLMENT_ENDPOINT_ID` tdtHQKHrAMllvoir, `ONDEMAND_SELFTEST_TOKEN` NGrV0CvtudZY76Yj). The credential offered as `VERCEL_TOKEN` on 2026-09-18 was the OnDemand API key (not a Vercel token) and was never sent to Vercel/GitHub. Operator-side deploy script ready: `scripts/vercel-file-deploy.mjs`. | `docs/audit/deployment-verification.md` §9 and Step 5 |
| Live contract baseline | emulator: 8 passed / 0 failed / 2 skipped, time-to-first-delta 1,435 ms (dev serverless emulator); function-runtime baseline: pending human deploy | **Emulator: 9 passed / 0 failed / 1 skipped** (step 8 workflow now executes the created workflow `6aace534859f7b0abb53d99a`; only step 4 skipped — the account exposes 0 agents). Latest run 2026-09-18T09:29:31Z: SSE time-to-first-delta **1,783 ms** (vs 1,435 ms reference, +348 ms run variance), workflow time-to-first-log **259 ms** (vs the 657 ms full-run reference). **Function-runtime baseline: still pending a human deploy** (`contract-baseline.vercel.json` not yet produced). | `docs/ondemand-workflows/contract-baseline.json` (07:31Z run, 9/0/1), `contract-baseline.emulator.json` (09:29Z run), `deployment-verification.md` §8–§9 |
| Gate 1 exit | yes (2026-09-18T04:51Z, live 10-step run with the user key) | **yes** — unchanged; later emulator runs confirm health all healthy | `docs/audit/gates.md` |
| Row 1 Seismic tool ID / status | `ondemand_tool_id: null`, status `registered-unverified`; adapter live-verified (24 h: 12 events, Gulf circle: 14); 10/10 citations via context injection (non-target architecture) | **`ondemand_tool_id: null`, status `registered-unverified`** (agent-tool creation is dashboard-only, `docs/ONDEMAND_API_CURRENT.md` §8; `GET /plugin/v1/list` → total 0 on 2026-09-18T09:18Z). Adapter live-verified again on 2026-09-18T09:28Z: 24 h M≥4.5 → **10 events**, Gulf circle 1,500 km/30 d M≥4 → **14 events**, unknown param → 400. The context-injection E2E is **retired**; its replacement, the capability loop (`mode: "capability-loop"`), selected and executed `earthquake.search` without any injected context (09:29:47Z). Registry row now carries `endpoint_url: null`, `endpoint_path: /api/sources/earthquakes`, `endpoint_url_pending_reason`. Dashboard pack: `docs/audit/gate3-row1-registration.md`. | `docs/audit/gate3-row1-earthquake-verification.md`, `deployment-verification.md` §6/§9, `src/registry/capabilities.json` |
| Row 2 Fires scaffold | not started | **Scaffolded and committed (`881e5a3`, 2026-09-18T09:27Z):** `server/sources/nasa-firms.js` (`fires.search`, NASA FIRMS area CSV API, bbox xor circle→bbox, sources VIIRS_NOAA20/NOAA21/SNPP/MODIS NRT, day_range 1–10, limit ≤ 200, 8 s timeout + one retry, provenance envelope, attribution `NASA FIRMS / LANCE`), route `GET /api/sources/fires` through the catch-all (function count 9), tool JSON `docs/ondemand-tools/fire_detection_search.json`, registry row `fires.search` (`registered-unverified`, `ondemand_tool_id: null`, `endpoint_url: null`, `endpoint_path: /api/sources/fires`), 13 tests (10 named cases + parity + deny-list/no-key). Key handling: `NASA_FIRMS_MAP_KEY` from env only → **503 `not_configured`** when absent (verified on the emulator 09:28:33Z); the 200 path is exercised only by stubbed tests until the key is provisioned. | `server/sources/nasa-firms.js`, `server/sources/nasa-firms.test.mjs`, `docs/ONDEMAND_PROXY_DESIGN.md` §12, `deployment-verification.md` §9 |
| Repo gates | green | **green at this state:** prettier `format:check` (940 files) ✔ · package boundaries ✔ · unit **4,144 pass / 0 fail** (1 pre-existing skip; 2 Node-24 allocation probes skipped on Node 22) · ondemand 185 ✔ · serverless 46 ✔ · sources 48 ✔ · `npm run build` ✔ (4.6 s) · function count **9** | this report (run 2026-09-18T09:45:28Z–09:46:22Z) |

## Vercel evidence (brief — full record in `deployment-verification.md` Step 5)

Project `ondemand-eand-spatial` / `prj_VbHbEhFSDFkdXCqlq8XqONoFWQHO` / team `schoolhack-web-team` (`team_aft8hHPiYnHp6I534L3DQScA`), vite, pro, git link `mk42-ai/ondemand-eand-spatial`, production branch `main`. Latest READY = `dpl_FKoHEVctqpLzNMaHV9sSRdmzcrw6` (exec-brief v1.4.0, alias `ondemand-eand-spatial-opal.vercel.app`) — **not this branch**; production `dpl_7CAoCxdEQRQ7W12mzwz4iGKDK4V9` BLOCKED; 9 git-source deployments BLOCKED (commit author `goose-live-build@users.noreply.github.com` has no Git account); **0 deployments of `ondemand-serverless`**. Env: 19 vars, 4 created this pass; `ONDEMAND_KNOWLEDGE_PLUGIN_IDS` and `ELEVENLABS_API_KEY` (`eazEtCde0VLgUCIO`) are present on the project but **deny-listed by this branch** (never read) — recommend removing `ELEVENLABS_API_KEY` from the project. **Stable deployment URL for this branch: none.** Ephemeral emulator previews used so far: `sb-5hfcfkb7a79s`, `sb-pg1bhjba1gcs`, `sb-26l9a6g6k6rt`, `sb-765981cquksp`, `sb-63r5liykgi73` (all `.vercel.run`, each expires ≤ 90 min).

## Remaining human steps

1. **Register the agent tools in the OnDemand dashboard** (no documented API): `earthquake_search` now, `fire_detection_search` once its key exists — REST agents pointing at the **stable** deployment URL (`https://<deployment>/api/sources/earthquakes`, `/api/sources/fires`), schemas `docs/ondemand-tools/earthquake_search.json` / `fire_detection_search.json`, steps in `docs/audit/gate3-row1-registration.md`; create the **OnDemand Spatial Intelligence Agent** and attach the nine skills (`docs/audit/dashboard-registration-pack.md`, `docs/ondemand-skills/`). Return the tool ids (`plugin-<digits>`) and the agent id → paste into `src/registry/capabilities.json` (`ondemand_tool_id`, `ondemand.agent.pluginId`) and env `ONDEMAND_SPATIAL_AGENT_ID`.
2. **Provision `NASA_FIRMS_MAP_KEY`** on the Vercel project (production + preview) for the Fires row (free key: https://firms.modaps.eosdis.nasa.gov/api/area/); until then `/api/sources/fires` answers `503 not_configured` by design.
3. **Grant `mk42-ai` write access to `bilawalsidhu/gods-eye-view`, or review/merge PR #657 from the fork** (`mk42-ai/gods-eye-view`, branch `ondemand-serverless`). Any further commits (including this report) must be pushed to the fork to refresh the PR — this run did not push.
4. **Create the function-runtime deployment from a local clone** — either `npx vercel deploy` (preview, project `ondemand-eand-spatial`, team `schoolhack-web-team`; commands in the previous run report) or `VERCEL_TOKEN=… node scripts/vercel-file-deploy.mjs --team team_aft8hHPiYnHp6I534L3DQScA --project prj_VbHbEhFSDFkdXCqlq8XqONoFWQHO --name ondemand-eand-spatial` — then run `GET /api/ondemand/selftest` against the READY URL with the project's `ONDEMAND_SELFTEST_TOKEN` and commit the result as `docs/ondemand-workflows/contract-baseline.vercel.json`; repeat the `deployment-verification.md` §9 table against that URL; update `endpoint_url` in the registry.

## SECURITY

An OnDemand API key was pasted into the chat transcript on 2026-09-18 (once as the OnDemand credential and once more mislabelled as a Vercel token) and was used only as a runtime-only environment value for the emulator/sandbox runs — never written to any file, commit, patch, archive or report. **It must be ROTATED after this session**, and the Vercel `ONDEMAND_API_KEY` entry (env id `wNMsI3sIbGdQb59Y`) updated if it is the same key. The selftest tokens used in this session were generated at runtime and discarded; the Vercel `ONDEMAND_SELFTEST_TOKEN` (env id `NGrV0CvtudZY76Yj`) was never read by this environment. `ELEVENLABS_API_KEY` and `ONDEMAND_KNOWLEDGE_PLUGIN_IDS` are deny-listed on this branch (`server/ondemand/deny-list.test.mjs`) and should be removed from the project's environment.
