{
  "components": {
    "schemas": {
      "Aircraft": {
        "additionalProperties": false,
        "description": "One aircraft as last reported by an ADS-B feed.\n\nImmutable. The store replaces the whole object on each update rather than mutating,\nso a snapshot handed to the API or the WebSocket hub can never change underneath its\nreader.",
        "properties": {
          "aircraft_class": {
            "$ref": "#/components/schemas/AircraftClass",
            "default": "unknown"
          },
          "barometric_altitude_m": {
            "anyOf": [
              {
                "maximum": 30000.0,
                "minimum": -500.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Barometric Altitude M"
          },
          "callsign": {
            "anyOf": [
              {
                "maxLength": 8,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Flight identifier as broadcast, whitespace stripped.",
            "title": "Callsign"
          },
          "category": {
            "anyOf": [
              {
                "pattern": "^[A-D][0-7]$",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "ADS-B emitter category, e.g. A3 for a large aeroplane.",
            "title": "Category"
          },
          "emergency": {
            "$ref": "#/components/schemas/EmergencyState",
            "default": "none"
          },
          "geometric_altitude_m": {
            "anyOf": [
              {
                "maximum": 30000.0,
                "minimum": -500.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Geometric Altitude M"
          },
          "ground_speed_mps": {
            "anyOf": [
              {
                "maximum": 1500.0,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Ground Speed Mps"
          },
          "icao24": {
            "description": "24-bit ICAO address, lowercase hex. The stable identity of the transponder.",
            "pattern": "^[0-9a-f]{6}$",
            "title": "Icao24",
            "type": "string"
          },
          "is_military": {
            "default": false,
            "title": "Is Military",
            "type": "boolean"
          },
          "kind": {
            "const": "aircraft",
            "default": "aircraft",
            "title": "Kind",
            "type": "string"
          },
          "message_source": {
            "default": "unknown",
            "description": "How the position was derived, verbatim from the feed: adsb_icao is a direct broadcast, mlat is multilaterated and less accurate, tisb_icao is rebroadcast ground data. Surfaced so the UI can be honest about accuracy.",
            "maxLength": 20,
            "title": "Message Source",
            "type": "string"
          },
          "messages_received": {
            "default": 0,
            "minimum": 0.0,
            "title": "Messages Received",
            "type": "integer"
          },
          "non_icao_address": {
            "default": false,
            "description": "The feed marked this address with a '~' prefix, meaning it is not a real ICAO allocation (typically a TIS-B ground vehicle or an ADS-R relay). The address is still a usable identity but should not be treated as an aircraft registration key.",
            "title": "Non Icao Address",
            "type": "boolean"
          },
          "observed_at": {
            "description": "When the feed generated the response carrying this record.",
            "format": "date-time",
            "title": "Observed At",
            "type": "string"
          },
          "on_ground": {
            "default": false,
            "description": "True when the feed reported altitude as 'ground' rather than a number.",
            "title": "On Ground",
            "type": "boolean"
          },
          "operator": {
            "anyOf": [
              {
                "maxLength": 120,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Operator"
          },
          "owner": {
            "anyOf": [
              {
                "maxLength": 120,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Owner"
          },
          "point": {
            "$ref": "#/components/schemas/Point"
          },
          "position_age_s": {
            "description": "Seconds between the position fix and observed_at, as reported by the feed's seen_pos. Drives the stale badge in the UI.",
            "minimum": 0.0,
            "title": "Position Age S",
            "type": "number"
          },
          "registered_country": {
            "anyOf": [
              {
                "maxLength": 80,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Registered Country"
          },
          "registration": {
            "anyOf": [
              {
                "maxLength": 12,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Tail number, from the feed's aircraft database rather than the air.",
            "title": "Registration"
          },
          "source": {
            "description": "Which adapter produced this record, e.g. adsb.lol. Shown as attribution.",
            "maxLength": 40,
            "minLength": 1,
            "title": "Source",
            "type": "string"
          },
          "squawk": {
            "anyOf": [
              {
                "pattern": "^[0-7]{4}$",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Squawk"
          },
          "track_deg": {
            "anyOf": [
              {
                "exclusiveMaximum": 360.0,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "True track over ground. Resolved from track, true_heading, mag_heading or dir, in that order of preference.",
            "title": "Track Deg"
          },
          "type_designator": {
            "anyOf": [
              {
                "maxLength": 4,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "ICAO type designator, e.g. B38M, GLF6, H60.",
            "title": "Type Designator"
          },
          "uses_privacy_address": {
            "default": false,
            "description": "Aircraft is broadcasting a privacy ICAO address. Not resolvable to an owner by design; we do not attempt to unmask it.",
            "title": "Uses Privacy Address",
            "type": "boolean"
          },
          "vertical_rate_mps": {
            "anyOf": [
              {
                "maximum": 200.0,
                "minimum": -200.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Bounds are deliberately wider than any real aircraft. Live feeds emit glitch values (a -32,640 ft/min record appeared in the 2026-08-19 military sample); those must validate and be visible, not silently drop the aircraft.",
            "title": "Vertical Rate Mps"
          }
        },
        "required": [
          "icao24",
          "point",
          "observed_at",
          "position_age_s",
          "source"
        ],
        "title": "Aircraft",
        "type": "object"
      },
      "AircraftClass": {
        "description": "How we classify an aircraft for display purposes.\n\nDerived, not broadcast. ``MILITARY`` comes from the feed's own database flag;\n``BUSINESS_JET`` from the type designator; ``ANONYMOUS`` marks an aircraft using a\nprivacy ICAO address, which we display as anonymous by design and never attempt to\nresolve to an owner.",
        "enum": [
          "unknown",
          "commercial",
          "business_jet",
          "general_aviation",
          "military",
          "helicopter",
          "anonymous"
        ],
        "title": "AircraftClass",
        "type": "string"
      },
      "AircraftSnapshot": {
        "additionalProperties": false,
        "description": "Every aircraft currently held, optionally filtered to a viewport.",
        "properties": {
          "aircraft": {
            "items": {
              "$ref": "#/components/schemas/Aircraft"
            },
            "title": "Aircraft",
            "type": "array"
          },
          "count": {
            "title": "Count",
            "type": "integer"
          }
        },
        "required": [
          "count",
          "aircraft"
        ],
        "title": "AircraftSnapshot",
        "type": "object"
      },
      "AttributionEntry": {
        "additionalProperties": false,
        "description": "One licence credit the UI is required to display.",
        "properties": {
          "licence": {
            "title": "Licence",
            "type": "string"
          },
          "source": {
            "title": "Source",
            "type": "string"
          },
          "text": {
            "title": "Text",
            "type": "string"
          },
          "url": {
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "source",
          "text",
          "url",
          "licence"
        ],
        "title": "AttributionEntry",
        "type": "object"
      },
      "Capabilities": {
        "additionalProperties": false,
        "description": "What this deployment can actually do, given its configuration.",
        "properties": {
          "attribution": {
            "items": {
              "$ref": "#/components/schemas/AttributionEntry"
            },
            "title": "Attribution",
            "type": "array"
          },
          "cesium_ion_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Cesium Ion Token"
          },
          "layers": {
            "items": {
              "$ref": "#/components/schemas/LayerCapability"
            },
            "title": "Layers",
            "type": "array"
          }
        },
        "required": [
          "layers",
          "attribution"
        ],
        "title": "Capabilities",
        "type": "object"
      },
      "EmergencyState": {
        "description": "ADS-B emergency/priority status, as broadcast.\n\nThe squawk codes carry the same meaning by convention: 7500 hijack, 7600 radio\nfailure, 7700 general emergency. Both are surfaced because an aircraft may set one\nwithout the other.",
        "enum": [
          "none",
          "general",
          "lifeguard",
          "minfuel",
          "nordo",
          "unlawful",
          "downed",
          "reserved"
        ],
        "title": "EmergencyState",
        "type": "string"
      },
      "FeedHealth": {
        "additionalProperties": false,
        "description": "Whether one upstream feed is currently working.\n\nSent to clients so the UI can show a per-source degraded banner instead of silently\ndisplaying a frozen layer as if it were live. ``consecutive_failures`` is included\nbecause one failed poll is noise and five is an outage.",
        "properties": {
          "consecutive_failures": {
            "default": 0,
            "minimum": 0.0,
            "title": "Consecutive Failures",
            "type": "integer"
          },
          "entity_count": {
            "minimum": 0.0,
            "title": "Entity Count",
            "type": "integer"
          },
          "healthy": {
            "title": "Healthy",
            "type": "boolean"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Error"
          },
          "last_success_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Success At"
          },
          "layer": {
            "$ref": "#/components/schemas/LayerName"
          },
          "poll_interval_seconds": {
            "exclusiveMinimum": 0.0,
            "title": "Poll Interval Seconds",
            "type": "number"
          },
          "rate_limited_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set when the upstream returned a throttling response. The UI says 'rate limited until X' rather than 'feed down', which is a materially different message: the data is fine, we are being asked to wait.",
            "title": "Rate Limited Until"
          },
          "source": {
            "title": "Source",
            "type": "string"
          }
        },
        "required": [
          "source",
          "layer",
          "healthy",
          "entity_count",
          "poll_interval_seconds"
        ],
        "title": "FeedHealth",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "Health": {
        "additionalProperties": false,
        "description": "Liveness plus per-feed health.",
        "properties": {
          "connected_clients": {
            "title": "Connected Clients",
            "type": "integer"
          },
          "feeds": {
            "items": {
              "$ref": "#/components/schemas/FeedHealth"
            },
            "title": "Feeds",
            "type": "array"
          },
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "status",
          "feeds",
          "connected_clients"
        ],
        "title": "Health",
        "type": "object"
      },
      "LayerCapability": {
        "additionalProperties": false,
        "description": "Whether one layer can run, and why not when it cannot.\n\nA merged layer also names its gated providers, as ``vessels/aishub`` and\n``vessels/aisstream``. The layer itself stays available because its keyless providers\ncarry it, and the entry for the gated provider says what is missing. Without that split\na vessel layer serving Baltic ships would read as entirely off.",
        "properties": {
          "available": {
            "title": "Available",
            "type": "boolean"
          },
          "layer": {
            "title": "Layer",
            "type": "string"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "required": [
          "layer",
          "available"
        ],
        "title": "LayerCapability",
        "type": "object"
      },
      "LayerName": {
        "enum": [
          "aircraft",
          "military",
          "vessels",
          "satellites",
          "events",
          "cameras"
        ],
        "type": "string"
      },
      "LayerSummary": {
        "additionalProperties": false,
        "description": "Counts and health per layer, for the layer rail and the degraded banners.",
        "properties": {
          "feeds": {
            "items": {
              "$ref": "#/components/schemas/FeedHealth"
            },
            "title": "Feeds",
            "type": "array"
          },
          "layers": {
            "additionalProperties": {
              "type": "integer"
            },
            "title": "Layers",
            "type": "object"
          },
          "providers": {
            "default": [],
            "items": {
              "$ref": "#/components/schemas/ProviderCoverage"
            },
            "title": "Providers",
            "type": "array"
          }
        },
        "required": [
          "layers",
          "feeds"
        ],
        "title": "LayerSummary",
        "type": "object"
      },
      "NavigationalStatus": {
        "description": "AIS navigational status, as broadcast, per ITU-R M.1371.\n\nWhat the master set on the transponder, so it disagrees with the vessel's actual\nbehaviour often enough that the card shows both this and the speed.",
        "enum": [
          "under_way_using_engine",
          "at_anchor",
          "not_under_command",
          "restricted_manoeuvrability",
          "constrained_by_draught",
          "moored",
          "aground",
          "engaged_in_fishing",
          "under_way_sailing",
          "towing_astern",
          "pushing_ahead",
          "ais_sart_active"
        ],
        "title": "NavigationalStatus",
        "type": "string"
      },
      "Point": {
        "additionalProperties": false,
        "description": "A position on the WGS84 ellipsoid.\n\n``altitude_m`` is metres above the ellipsoid, not above ground and not above mean sea\nlevel. It is ``None`` when the source does not report altitude, and ``0.0`` only when\nthe source actively says the object is at the surface.",
        "properties": {
          "altitude_m": {
            "anyOf": [
              {
                "maximum": 40000000.0,
                "minimum": -500.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Altitude M"
          },
          "lat": {
            "maximum": 90.0,
            "minimum": -90.0,
            "title": "Lat",
            "type": "number"
          },
          "lon": {
            "maximum": 180.0,
            "minimum": -180.0,
            "title": "Lon",
            "type": "number"
          }
        },
        "required": [
          "lon",
          "lat"
        ],
        "title": "Point",
        "type": "object"
      },
      "ProviderCoverage": {
        "additionalProperties": false,
        "description": "One provider's contribution to a merged layer, for the last cycle.\n\nADR 010 asks for two things this carries. ``exclusive`` is the provider-attributable\ncount, so \"ships only this network can see\" is measured rather than asserted. ``error``\nnames a provider that dropped out, which is how the layer reports itself degraded\ninstead of quietly covering less.\n\nThis is coverage, not corroboration, and the two must not be confused: under R1 in\n``docs/pending-decisions.md`` three providers reporting one ship are still one origin,\nbecause they are repeating one AIS broadcast. Nothing here may be counted as\nindependent sources.",
        "properties": {
          "error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Error"
          },
          "exclusive": {
            "title": "Exclusive",
            "type": "integer"
          },
          "layer": {
            "title": "Layer",
            "type": "string"
          },
          "provider": {
            "title": "Provider",
            "type": "string"
          },
          "records": {
            "title": "Records",
            "type": "integer"
          }
        },
        "required": [
          "layer",
          "provider",
          "records",
          "exclusive"
        ],
        "title": "ProviderCoverage",
        "type": "object"
      },
      "Satellite": {
        "additionalProperties": false,
        "description": "One GP element set, keyed on the NORAD catalogue number.\n\nImmutable, like every entity here. Field names are ours; the mapping from the OMM\nkeyword to each one is in the adapter and only there.",
        "properties": {
          "arg_of_pericenter_deg": {
            "description": "Degrees.",
            "maximum": 360.0,
            "minimum": 0.0,
            "title": "Arg Of Pericenter Deg",
            "type": "number"
          },
          "bstar": {
            "description": "B* drag term, inverse earth radii. Zero on the recorded TDRS 3 record, which is a real value for a geostationary object and not a missing one.",
            "title": "Bstar",
            "type": "number"
          },
          "classification_type": {
            "description": "U unclassified, C classified, S secret. Both sampled records read U.",
            "pattern": "^[UCS]$",
            "title": "Classification Type",
            "type": "string"
          },
          "eccentricity": {
            "description": "Dimensionless. Outside 0 to 1 is SatRecError 1, an element set SGP4 cannot propagate, so it is refused here.",
            "exclusiveMaximum": 1.0,
            "minimum": 0.0,
            "title": "Eccentricity",
            "type": "number"
          },
          "element_set_no": {
            "description": "Element set number. Both recorded records read 999.",
            "minimum": 0.0,
            "title": "Element Set No",
            "type": "integer"
          },
          "ephemeris_type": {
            "description": "Always 0 on CelesTrak GP data, which means SGP4. Carried because it is part of the element set the propagator is handed.",
            "maximum": 9.0,
            "minimum": 0.0,
            "title": "Ephemeris Type",
            "type": "integer"
          },
          "epoch": {
            "description": "The instant these elements describe, UTC, serialised with a Z suffix.",
            "format": "date-time",
            "title": "Epoch",
            "type": "string"
          },
          "fetched_at": {
            "description": "When we fetched this element set. Distinct from epoch, which is when the elements were fitted: the fetch says how current our copy is, the epoch says how current the orbit determination is, and only the second one decides whether a propagated position is worth drawing.",
            "format": "date-time",
            "title": "Fetched At",
            "type": "string"
          },
          "group": {
            "description": "The CelesTrak group this element set was fetched from, e.g. stations. Provenance, and the reason two overlapping groups cannot double-count an object: the store is keyed on the catalogue number, not on the group.",
            "maxLength": 40,
            "minLength": 1,
            "title": "Group",
            "type": "string"
          },
          "inclination_deg": {
            "description": "Degrees.",
            "maximum": 180.0,
            "minimum": 0.0,
            "title": "Inclination Deg",
            "type": "number"
          },
          "kind": {
            "const": "satellite",
            "default": "satellite",
            "title": "Kind",
            "type": "string"
          },
          "mean_anomaly_deg": {
            "description": "Degrees.",
            "maximum": 360.0,
            "minimum": 0.0,
            "title": "Mean Anomaly Deg",
            "type": "number"
          },
          "mean_motion": {
            "description": "Revolutions per day, no conversion from the wire. At or below zero is SatRecError 2 and cannot be propagated, so it is refused rather than stored.",
            "exclusiveMinimum": 0.0,
            "title": "Mean Motion",
            "type": "number"
          },
          "mean_motion_ddot": {
            "description": "Second derivative, already sixthed by the provider. Zero on both recorded records.",
            "title": "Mean Motion Ddot",
            "type": "number"
          },
          "mean_motion_dot": {
            "description": "First derivative of mean motion, already halved by the provider. Negative on the recorded TDRS 3 record. Goes to satellite.js verbatim.",
            "title": "Mean Motion Dot",
            "type": "number"
          },
          "norad_cat_id": {
            "description": "NORAD catalogue number, the identity of the object. 1 to 9 digits, never zero-padded to a fixed width.",
            "maximum": 999999999.0,
            "minimum": 1.0,
            "title": "Norad Cat Id",
            "type": "integer"
          },
          "object_id": {
            "anyOf": [
              {
                "maxLength": 20,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "International designator, e.g. 1998-067A. Also absent for analyst objects.",
            "title": "Object Id"
          },
          "object_name": {
            "anyOf": [
              {
                "maxLength": 80,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Name as published, e.g. 'ISS (ZARYA)'. Absent for analyst objects in the 80000 series, which carry no name at all.",
            "title": "Object Name"
          },
          "ra_of_asc_node_deg": {
            "description": "Right ascension of the ascending node, degrees. Not a compass bearing, so it is bounded explicitly rather than typed as one.",
            "maximum": 360.0,
            "minimum": 0.0,
            "title": "Ra Of Asc Node Deg",
            "type": "number"
          },
          "rev_at_epoch": {
            "description": "Revolution number at epoch. 58157 on the recorded ISS record, so the 5-digit TLE column is already at its limit here too.",
            "minimum": 0.0,
            "title": "Rev At Epoch",
            "type": "integer"
          },
          "source": {
            "description": "Which adapter produced this record, e.g. celestrak. Shown as attribution.",
            "maxLength": 40,
            "minLength": 1,
            "title": "Source",
            "type": "string"
          }
        },
        "required": [
          "norad_cat_id",
          "classification_type",
          "epoch",
          "mean_motion",
          "eccentricity",
          "inclination_deg",
          "ra_of_asc_node_deg",
          "arg_of_pericenter_deg",
          "mean_anomaly_deg",
          "bstar",
          "mean_motion_dot",
          "mean_motion_ddot",
          "ephemeris_type",
          "element_set_no",
          "rev_at_epoch",
          "group",
          "fetched_at",
          "source"
        ],
        "title": "Satellite",
        "type": "object"
      },
      "SatelliteElements": {
        "additionalProperties": false,
        "description": "Cached OMM element sets, with when each group was last fetched.\n\n``fetched`` is the evidence for the two-hour floor: it is the instant of the fetch,\nwhich is a different thing from an element set's epoch. One record per catalogue number,\nfreshest epoch winning, because CelesTrak groups overlap.",
        "properties": {
          "count": {
            "title": "Count",
            "type": "integer"
          },
          "fetched": {
            "additionalProperties": {
              "format": "date-time",
              "type": "string"
            },
            "title": "Fetched",
            "type": "object"
          },
          "satellites": {
            "items": {
              "$ref": "#/components/schemas/Satellite"
            },
            "title": "Satellites",
            "type": "array"
          }
        },
        "required": [
          "count",
          "fetched",
          "satellites"
        ],
        "title": "SatelliteElements",
        "type": "object"
      },
      "SatelliteSnapshot": {
        "additionalProperties": false,
        "description": "Every satellite currently held.\n\nNo bounding box, and there is nothing to filter on: a satellite record carries orbital\nelements rather than a position, and the browser propagates it. Filtering here would\nmean running SGP4 on the server for every object on every request.",
        "properties": {
          "count": {
            "title": "Count",
            "type": "integer"
          },
          "satellites": {
            "items": {
              "$ref": "#/components/schemas/Satellite"
            },
            "title": "Satellites",
            "type": "array"
          }
        },
        "required": [
          "count",
          "satellites"
        ],
        "title": "SatelliteSnapshot",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "Vessel": {
        "additionalProperties": false,
        "description": "One vessel as last reported by an AIS feed.\n\nImmutable, like every entity here: the store replaces the whole object on update, so a\nsnapshot handed to the API or the WebSocket hub cannot change underneath its reader.\n\n``point.altitude_m`` is always ``None``. AIS reports no altitude, and 0.0 would be this\ncontract asserting the source said \"at the surface\" when it said nothing at all.",
        "properties": {
          "beam_m": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "maximum": 126.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Overall beam, summed in the adapter from reference points C and D.",
            "title": "Beam M"
          },
          "call_sign": {
            "anyOf": [
              {
                "maxLength": 7,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Radio call sign. The feed sends an empty string when it has none, which the adapter maps to None.",
            "title": "Call Sign"
          },
          "course_over_ground_deg": {
            "anyOf": [
              {
                "exclusiveMaximum": 360.0,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Course over ground, the direction the vessel is actually moving. 0.0 is a real course due north; 360.0 on the wire means not available and is mapped to None before this bound is applied.",
            "title": "Course Over Ground Deg"
          },
          "destination": {
            "anyOf": [
              {
                "maxLength": 20,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Destination as typed by the crew, free text and often a route like FIHEL<>FIMHQ<>SESTO. Empty on 91 of 950 live records, mapped to None.",
            "title": "Destination"
          },
          "draught_m": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "maximum": 25.5,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Maximum present static draught in metres. Decimetres on the AIS endpoint and metres on the port-call endpoint, so the conversion is per endpoint in the adapter. 0 means not available.",
            "title": "Draught M"
          },
          "eta": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/VesselEta"
              },
              {
                "type": "null"
              }
            ],
            "description": "Estimated time of arrival, decoded from the packed AIS field. Carries no year, because the wire field has none."
          },
          "imo": {
            "anyOf": [
              {
                "maximum": 9999999.0,
                "minimum": 1000000.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "IMO ship identification number, seven digits. 0 means not available on the wire and so does anything outside the seven-digit range: the live feed carried values up to 912974400, which is not an IMO number. Both map to None in the adapter, because dropping a real ship over one junk optional field would be worse than not knowing its IMO.",
            "title": "Imo"
          },
          "kind": {
            "const": "vessel",
            "default": "vessel",
            "title": "Kind",
            "type": "string"
          },
          "length_m": {
            "anyOf": [
              {
                "exclusiveMinimum": 0.0,
                "maximum": 1022.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Overall length, summed in the adapter from the AIS reference points A and B. The bound is what those two 9-bit fields can hold, not what a real ship is, so a junk dimension does not drop a real vessel.",
            "title": "Length M"
          },
          "mmsi": {
            "description": "Maritime Mobile Service Identity, nine digits, zero-padded. The vessel merge key under ADR 010, and only ever a ship-station MMSI: the first three digits are the ITU MID, which gives the flag state in phase 5 with no registry call.",
            "pattern": "^[0-9]{9}$",
            "title": "Mmsi",
            "type": "string"
          },
          "name": {
            "anyOf": [
              {
                "maxLength": 20,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Vessel name as broadcast, capped at 20 characters upstream. None when no static record joined to the position, which was 108 of 1,058 on 2026-08-19.",
            "title": "Name"
          },
          "navigational_status": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/NavigationalStatus"
              },
              {
                "type": "null"
              }
            ],
            "description": "What the master set on the transponder. None for the undefined code and the reserved ones."
          },
          "observed_at": {
            "description": "When the provider generated the response carrying this record.",
            "format": "date-time",
            "title": "Observed At",
            "type": "string"
          },
          "point": {
            "$ref": "#/components/schemas/Point"
          },
          "position_age_s": {
            "description": "Seconds between the position fix and observed_at. Per ADR 010 every record carries how old its report is, because recency is what resolves a conflict between two providers and the card has to be able to show why. Digitraffic's default query window is 24 hours, so an age of tens of thousands of seconds is a real answer and not a fault.",
            "minimum": 0.0,
            "title": "Position Age S",
            "type": "number"
          },
          "rate_of_turn_deg_per_min": {
            "anyOf": [
              {
                "maximum": 710.0,
                "minimum": -710.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Rate of turn, decoded from the signed ROT_AIS wire value where ROT_AIS is 4.733 times the square root of the rate. Negative is to port. None when the feed said not available (-128) and also at +/-127, where the feed says the vessel is turning faster than 5 degrees per 30 seconds without giving a rate.",
            "title": "Rate Of Turn Deg Per Min"
          },
          "ship_type": {
            "anyOf": [
              {
                "maximum": 99.0,
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "AIS ship and cargo type code, 1 to 99. 0 means not available.",
            "title": "Ship Type"
          },
          "source": {
            "description": "Which provider supplied this record, e.g. digitraffic. Per ADR 010 this is per record and never per layer: a merged store that cannot say which network saw a given ship is unauditable. The full list of providers that saw it rides on the merge result in services/union.py, and under R1 in docs/pending-decisions.md that list is still one origin for corroboration.",
            "maxLength": 40,
            "minLength": 1,
            "title": "Source",
            "type": "string"
          },
          "speed_over_ground_mps": {
            "anyOf": [
              {
                "maximum": 51.4,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Speed over ground, converted from knots in the adapter.",
            "title": "Speed Over Ground Mps"
          },
          "true_heading_deg": {
            "anyOf": [
              {
                "exclusiveMaximum": 360.0,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Where the bow points, which is not the course over ground: a vessel in a tideway carries a heading well off its track. 511 means not available.",
            "title": "True Heading Deg"
          }
        },
        "required": [
          "mmsi",
          "point",
          "observed_at",
          "position_age_s",
          "source"
        ],
        "title": "Vessel",
        "type": "object"
      },
      "VesselEta": {
        "additionalProperties": false,
        "description": "A decoded AIS estimated time of arrival.\n\n**Not a datetime, and it must never be turned into one.** The wire field is a 20-bit\npacked integer holding month, day, hour and minute, and it carries no year at all, so\nany year is a guess. Storing the raw integer as though it were an epoch dates every\nvessel to 1970.",
        "properties": {
          "day": {
            "maximum": 31.0,
            "minimum": 1.0,
            "title": "Day",
            "type": "integer"
          },
          "hour": {
            "maximum": 23.0,
            "minimum": 0.0,
            "title": "Hour",
            "type": "integer"
          },
          "minute": {
            "maximum": 59.0,
            "minimum": 0.0,
            "title": "Minute",
            "type": "integer"
          },
          "month": {
            "maximum": 12.0,
            "minimum": 1.0,
            "title": "Month",
            "type": "integer"
          }
        },
        "required": [
          "month",
          "day",
          "hour",
          "minute"
        ],
        "title": "VesselEta",
        "type": "object"
      },
      "VesselSnapshot": {
        "additionalProperties": false,
        "description": "Every vessel currently held, optionally filtered to a viewport.",
        "properties": {
          "count": {
            "title": "Count",
            "type": "integer"
          },
          "vessels": {
            "items": {
              "$ref": "#/components/schemas/Vessel"
            },
            "title": "Vessels",
            "type": "array"
          }
        },
        "required": [
          "count",
          "vessels"
        ],
        "title": "VesselSnapshot",
        "type": "object"
      }
    }
  },
  "info": {
    "description": "Live aircraft, ships, satellites, public cameras and geolocated events, served from real public feeds. Every response names its source and licence.",
    "summary": "Real public data on an interactive globe",
    "title": "Tracker",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/api/aircraft": {
      "get": {
        "description": "Aircraft currently known to the server.\n\nMilitary aircraft are held in their own store because they come from a separate\nworldwide endpoint rather than the viewport query, and merging them into one store\nwould let a global feed evict the local one every poll.",
        "operationId": "list_aircraft_api_aircraft_get",
        "parameters": [
          {
            "in": "query",
            "name": "west",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 180.0,
                  "minimum": -180.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "West"
            }
          },
          {
            "in": "query",
            "name": "south",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 90.0,
                  "minimum": -90.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "South"
            }
          },
          {
            "in": "query",
            "name": "east",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 180.0,
                  "minimum": -180.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "East"
            }
          },
          {
            "in": "query",
            "name": "north",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 90.0,
                  "minimum": -90.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "North"
            }
          },
          {
            "in": "query",
            "name": "military_only",
            "required": false,
            "schema": {
              "default": false,
              "title": "Military Only",
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AircraftSnapshot"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Aircraft",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/aircraft/{icao24}": {
      "get": {
        "description": "One aircraft by ICAO address, from either store. ``null`` when not currently seen.",
        "operationId": "get_aircraft_api_aircraft__icao24__get",
        "parameters": [
          {
            "in": "path",
            "name": "icao24",
            "required": true,
            "schema": {
              "title": "Icao24",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Aircraft"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Get Aircraft Api Aircraft  Icao24  Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Aircraft",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/capabilities": {
      "get": {
        "description": "Which layers this deployment can serve, and the attributions it must display.\n\nThe Cesium ion token is returned to the browser deliberately: it is a client-side\ntoken by design, scoped to the assets it can stream, unlike the feed keys which never\nleave the server.",
        "operationId": "capabilities_api_capabilities_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Capabilities"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Capabilities",
        "tags": [
          "meta"
        ]
      }
    },
    "/api/health": {
      "get": {
        "description": "Liveness and upstream feed health.\n\nReports ``ok`` whenever the process is serving, even with every feed down, because\nthis is a liveness probe rather than a data-quality judgement. Feed trouble is\nvisible in ``feeds`` and is what the UI banners read.",
        "operationId": "health_api_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Health",
        "tags": [
          "meta"
        ]
      }
    },
    "/api/layers": {
      "get": {
        "description": "Entity counts per layer plus upstream health, for the layer rail.",
        "operationId": "layer_summary_api_layers_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/LayerSummary"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Layer Summary",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/satellites": {
      "get": {
        "description": "Satellites currently held, one record per NORAD catalogue number.",
        "operationId": "list_satellites_api_satellites_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SatelliteSnapshot"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Satellites",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/satellites/elements": {
      "get": {
        "description": "Cached orbital element sets, served without touching CelesTrak.\n\nDeclared before nothing else on this prefix by design: the store read above answers\n``/api/satellites`` and this answers the element cache, which is what a client\npropagating orbits itself needs.",
        "operationId": "satellite_elements_api_satellites_elements_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SatelliteElements"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Satellite Elements",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/vessels": {
      "get": {
        "description": "Vessels currently known to the server, merged across every reporting provider.\n\nOne record per MMSI whatever the provider count, per ADR 010, and each record names the\nprovider whose report supplied it and how old that report was.",
        "operationId": "list_vessels_api_vessels_get",
        "parameters": [
          {
            "in": "query",
            "name": "west",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 180.0,
                  "minimum": -180.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "West"
            }
          },
          {
            "in": "query",
            "name": "south",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 90.0,
                  "minimum": -90.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "South"
            }
          },
          {
            "in": "query",
            "name": "east",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 180.0,
                  "minimum": -180.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "East"
            }
          },
          {
            "in": "query",
            "name": "north",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maximum": 90.0,
                  "minimum": -90.0,
                  "type": "number"
                },
                {
                  "type": "null"
                }
              ],
              "title": "North"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VesselSnapshot"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Vessels",
        "tags": [
          "entities"
        ]
      }
    },
    "/api/vessels/{mmsi}": {
      "get": {
        "description": "One vessel by MMSI. ``null`` when not currently seen.",
        "operationId": "get_vessel_api_vessels__mmsi__get",
        "parameters": [
          {
            "in": "path",
            "name": "mmsi",
            "required": true,
            "schema": {
              "title": "Mmsi",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "anyOf": [
                    {
                      "$ref": "#/components/schemas/Vessel"
                    },
                    {
                      "type": "null"
                    }
                  ],
                  "title": "Response Get Vessel Api Vessels  Mmsi  Get"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Vessel",
        "tags": [
          "entities"
        ]
      }
    }
  }
}
